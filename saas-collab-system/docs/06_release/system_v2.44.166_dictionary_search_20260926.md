# V2.44.166 基础档案字典搜索

## 版本登记

- 登记日期：2026-09-26（Asia/Shanghai）
- 状态：`REGISTERED_FOR_CONTROLLED_RELEASE`；部署状态以虚拟机发布通道和双账本实际结果为准
- 发布通道：虚拟机功能发布通道
- 前序版本：`V2.44.165`，虚拟机统一、共享账本与 `v2.44.165-deployed` 标签均指向 `51bb9ea4b736fbeac777888ee0aa16e287c059fd`
- 候选 PR：[#222](https://github.com/dfcy-team/dfcy/pull/222)，候选分支 `codex/dictionary-settings-search`
- 最终发布 SHA：以受保护 `main` 的实际合并提交为准，不以候选分支 SHA 代替
- 数据库迁移、菜单权限和路由变更：无

## 变更范围

1. 属性设置和颜色设置增加按编码或名称即时筛选，支持清空搜索并显示无匹配提示。
2. 规格设置沿用左侧分类树，在搜索中增加分类路径、规格编码、名称和选项值匹配；保留匹配末级分类的上级节点并显示匹配数量。
3. 搜索只改变页面展示，不修改字典数据或现有导出范围；切换菜单时清空搜索条件。

## 候选验收与发布边界

- 本地前端全量测试：127 个文件、769 项通过；生产构建和菜单权限快照检查通过。
- PR #222 须在最新候选提交通过完整 CI，发布通道核查差异后经受保护 PR 合并。
- 仅以最终主干合并 SHA 构建不可变镜像并走受控虚拟机部署；部署后核对备份、健康检查、四个应用容器 revision、部署标签及统一/共享账本一致，方可登记为已部署。
