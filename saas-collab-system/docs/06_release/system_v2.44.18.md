# SaaS 协同系统 V2.44.18 平台商品明细导入修复登记

- 登记日期：2026-08-14
- 父版本：V2.44.17
- 发布状态：已登记并部署

本版本只修复平台商品明细数据的 CSV/XLSX 导入后端；前端、菜单、路由和数据库结构均未调整。

- 旧 SKU 与新 SKU 匹配统一执行 Unicode NFKC 规范化、移除零宽/格式字符、去除首尾空白并忽略大小写。
- 新 SKU 有值时继续优先使用新 SKU；新旧 SKU 均为空时仍拒绝该行。
- 规范化后若匹配到多个内部 SKU，明确返回歧义错误，不自动选择，避免错误关联。
- 有效行按平台、店铺、变体 ID 幂等批量写入；无效行跳过并保留行号和错误信息，不再因单行错误回滚整份文件。
- 同一文件出现重复平台/店铺/变体键时保留最后一条有效记录，避免批量唯一键冲突。
- 返回结果新增 `skipped` 与 `partial_success`，便于现有导入状态界面展示部分成功结果。
- 菜单未修改，菜单 SHA256 仍为 `c4919748b05f596ede323f0dfc86e7d58bcae40abb366b952ffb00889a4b3ab9`。
- 后端镜像：`saas-collab-backend:v2.44.18@sha256:146b9fa3852289192462561987711403153ef93dab8b0c8093385ad29a55508b`。
- 前端继续使用：`saas-collab-frontend:v2.44.16`。
- 导入模块 SHA256：`58b247244f8fb2b5ea039198259e39310d77c57fc26c9e219df789b290d21eb9`。

附件预检（SHA256 `0150691787a12388ffbbceeeb9e1db68f40eb36f0fce4f4af09920746957803b`）：

- 总行数：48,746。
- 修复前有效：35,374；错误：13,372。
- 修复后有效：35,461；错误：13,285；新增恢复 87 行。
- 示例 `HY040-Pink-6KG` 尾部零宽字符及大小写差异已可正确匹配。
- 附件 48,746 个平台/店铺/变体组合均唯一，无文件内重复键。
- 剩余错误为真实业务数据问题（例如 `#N/A`、只填旧 SPU 未填 SKU、国家代码与店铺国家不一致），系统会跳过并返回具体行号，不会错误绑定。

验证：平台商品明细后端测试 12 项通过；本地及生产 `manage.py check` 通过；部署后认证接口返回预期 HTTP 401（路由可达）；生产容器附件 dry-run 约 10 秒完成。

