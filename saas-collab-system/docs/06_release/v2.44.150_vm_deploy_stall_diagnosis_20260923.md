# V2.44.150 虚拟机部署卡顿诊断（2026-09-23）

状态：诊断中，**不得登记为已部署**。第一次发布作业 `35820841491` 的质量门与镜像构建通过，虚拟机入口在约 29 分钟后报 `backend image pull failed`，发生在备份、迁移和服务切换之前。第二次作业 `35823928102` 从 06:01:54 UTC 起停留在 `Invoke the fixed forced-command entrypoint`，07:12:38 UTC 虚拟机重启时 SSH 连接中断，作业失败。第二次的具体服务器内阶段未知，不得据此断言数据库死锁或镜像拉取是唯一原因。`v2.44.150-deployed` 标签不存在。

仓库镜像摘要从非生产环境可以读取，GitHub 官方状态页显示 Packages 与 Actions 正常；这只能排除明显的镜像不存在及全局服务故障，不能证明虚拟机到 GHCR 的链路正常。发布脚本原先隐藏 `docker pull` 的全部输出、没有拉取超时；CI 的 SSH 进程无等待上限。这是卡顿难以定位的直接可观测性缺口。

## 虚拟机与数据库只读检查

由具备生产只读权限的运维人员在相应主机执行，回传结果时去除令牌、地址和凭据：

1. 应用虚拟机：查看 `production-deploy`、`docker pull`、`docker login` 进程及运行时长；查看 Docker 服务状态、最近日志、`/var/lib/docker` 可用空间；对 `ghcr.io/v2/` 做有时限的 DNS/TLS 连通检查。未经确认远端阶段，不要取消 GitHub 作业或重启 Docker。
2. 数据库虚拟机：只读检查 `SHOW FULL PROCESSLIST`、`performance_schema.data_lock_waits` 以及 `performance_schema.metadata_locks` 中 `LOCK_STATUS='PENDING'` 的记录。只有证明已进入备份或迁移阶段，且存在对应等待链时，才把数据库锁列为本次卡顿原因。
3. 核对生产发布账本的 current SHA、迁移记录与应用健康后，才能判定第二次作业是否曾越过镜像拉取阶段。不要仅凭 GitHub 步骤成功或失败就认为服务已经切换。

## 重启后现场定位（2026-09-23）

应用虚拟机内网地址是 `192.168.174.131`，本机映射 `192.168.2.10:22131` 指向其 SSH `22` 端口；两个入口当前返回同一 SSH 主机公钥。现场 `authorized_keys` 的强制命令实际指向 `/opt/saas-collab/release-control/unified/ci-control/bin/developer-a-ci-dispatch`。上一级 `unified` 目录属于另一套共享发布资料，不是本次 CI 控制根；检查其 `current.json` 或 `bin/production-baseline-check` 得到“文件不存在”不能否定 `ci-control` 的运行时基线。此前按上一级目录指导人工核对是路径错误。

重启后的只读 CI 检查 `35832696714` 在 `ci-control` 入口返回 `PRODUCTION_BASELINE_RUNTIME=PASS`；这只证明控制面基线，不证明 V2.44.150 已部署。现场应用 frontend、backend、Celery、Beat、Redis 容器均在运行。随后在 VM 上只读核对 `ci-control/current.json`：`release_sha=29cdd5a1ffdc6ce9098e430e916f0daa0fdf4550`，`completed_at=2026-09-23T04:03:47Z`；前后端容器的 `org.opencontainers.image.revision` 均为同一 SHA，镜像摘要也与账本一致。该 SHA 对应 `v2.44.149-deployed`，不是 V2.44.150 的 `98b2648690f08d704014f365b15f8810f090259b`。因此当前运行的仍是 V2.44.149，V2.44.150 未切换；下一步应聚焦发布链路，不能把模板未更新归因于浏览器缓存或成本导入实现。

本次代码修复候选只为后续安装提供分段提示、GHCR 登录 90 秒超时、各镜像默认 900 秒（可配置 60–1800 秒）超时，并将拉取失败归类为鉴权、网络、存储、Docker 守护进程或未知。Docker 原始错误只保留在虚拟机 root 可读的独立日志，不写入 CI。该候选**未安装到虚拟机，也不能追溯诊断正在运行的旧版脚本**。
