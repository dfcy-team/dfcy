# V2.44.150 虚拟机部署卡顿诊断（2026-09-23）

状态：诊断中，**不得登记为已部署**。第一次发布作业 `35820841491` 的质量门与镜像构建通过，虚拟机入口在约 29 分钟后报 `backend image pull failed`，发生在备份、迁移和服务切换之前。第二次作业 `35823928102` 从 06:01:54 UTC 起停留在 `Invoke the fixed forced-command entrypoint`，截至本记录未返回。第二次的具体服务器内阶段未知，不得据此断言数据库死锁或镜像拉取是唯一原因。

仓库镜像摘要从非生产环境可以读取，GitHub 官方状态页显示 Packages 与 Actions 正常；这只能排除明显的镜像不存在及全局服务故障，不能证明虚拟机到 GHCR 的链路正常。发布脚本原先隐藏 `docker pull` 的全部输出、没有拉取超时；CI 的 SSH 进程无等待上限。这是卡顿难以定位的直接可观测性缺口。

## 虚拟机与数据库只读检查

由具备生产只读权限的运维人员在相应主机执行，回传结果时去除令牌、地址和凭据：

1. 应用虚拟机：查看 `production-deploy`、`docker pull`、`docker login` 进程及运行时长；查看 Docker 服务状态、最近日志、`/var/lib/docker` 可用空间；对 `ghcr.io/v2/` 做有时限的 DNS/TLS 连通检查。未经确认远端阶段，不要取消 GitHub 作业或重启 Docker。
2. 数据库虚拟机：只读检查 `SHOW FULL PROCESSLIST`、`performance_schema.data_lock_waits` 以及 `performance_schema.metadata_locks` 中 `LOCK_STATUS='PENDING'` 的记录。只有证明已进入备份或迁移阶段，且存在对应等待链时，才把数据库锁列为本次卡顿原因。
3. 核对生产发布账本的 current SHA、迁移记录与应用健康后，才能判定第二次作业是否曾越过镜像拉取阶段。不要仅凭 GitHub 步骤仍在运行就认为服务已经切换。

本次代码修复候选只为后续安装提供分段提示、GHCR 登录 90 秒超时、各镜像默认 900 秒（可配置 60–1800 秒）超时，并将拉取失败归类为鉴权、网络、存储、Docker 守护进程或未知。Docker 原始错误只保留在虚拟机 root 可读的独立日志，不写入 CI。该候选**未安装到虚拟机，也不能追溯诊断正在运行的旧版脚本**。
