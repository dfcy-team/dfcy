# 开发A两项候选合并与安全整改

本分支基于已部署 V2.44.79 / `91726fea36ee3c31195d61a9b4579dea69850e09`。
保留极风仓库级凭据授权与店铺手动回调两项功能；没有纳入尚未部署的 V2.44.80 商品变更。
原始候选交接说明为输入历史，不表示本次已发布。

## 范围

- A1：仓库绑定重放不得再次写入凭据或重置已消费 Token；幂等载荷包含凭据变化的受保护摘要，并保留正常同配置编辑。
- A2：修正极风测试夹具的凭据扫描命中，不关闭或放宽扫描规则。
- B1：平台返回的现有授权必须属于本次店铺和配置，且发起用户仍有实际目标权限；错误回调不得修改旧授权，并清理未落库的新托管引用。
- 后端路由和前端 API 函数保留两包新增入口；共享弹窗和测试按共同基线合并。
- 数据库仍只有原极风候选的 `integrations.0025_warehouse_credentials`，修复本身不新增迁移。

## 发布边界

本分支仅为可复核候选。没有生产数据库写入、真实店铺授权、一次性 Token 消耗、镜像切换或版本登记。
发布前重新核对实际部署基线，走受控 CI、备份及 MySQL 迁移演练，不直接以候选文件覆盖应用目录。
数据库新增字段前向兼容保留；应用回退使用发布前经核定的不可变镜像，停用本次新增的极风任务，不自动反向删除授权数据。

## 验证

- 仓库相关后端首轮48项通过；补充实际换绑入口后，最终8项幂等专项通过。
- OAuth、凭据、运行配置相关8个测试文件43项通过，包含7项新增安全用例。
- 前端按锁文件 `npm ci --ignore-scripts` 安装后，全量489项通过、1项失败；失败为 `development-competitor.spec.js:56` 的既有 Mock 测试，已在未修改的 V2.44.79 基线复现。本次不修订无关模块，不声称全量测试全绿。
- 生产构建、CI guard、Django check、迁移漂移及差异空白检查通过。菜单权限快照保持100菜单、133路由。
- 后端采用隔离 SQLite；未执行真实 MySQL 并发压测或迁移演练。所有平台响应及凭据均为测试数据，不代表真实平台连通验收。

最终修复提交和交付摘要见随包整改交接说明。
