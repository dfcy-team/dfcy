# V2.44.124 Shopee／极风授权自动续期

## 父版本

- 目标父版本：`v2.44.123-deployed`
- 父提交：`641090eb62418852b370bed18fa1872c18c0ca37`
- 本版本必须在 V2.44.123 部署并复核后发布。

## 发布目标

- 对临近到期的 Shopee 店铺授权与极风 WMS 仓库授权执行受控自动续期。
- 每次处理前重新核对平台开关、授权状态、操作者权限、数据范围和运行中同步。
- 以授权引用代次生成不可逆请求键，失败或结果不确定时不自动重放。
- 管理页通过双人审批开关功能，并展示维护任务最近心跳。

## 变更边界

- 数据库迁移：`integrations.0029_automaticrefreshattempt`。
- 无新菜单、路由或权限；菜单与路由数量保持 `104 / 136`。
- 默认关闭自动续期；发布本身不修改生产运行配置，也不触发授权刷新。
- 不记录或返回令牌、密钥、托管引用和上游异常原文。
- 不启用、停用或执行同步任务。

## 验收要求

- 平台白名单、严格布尔配置、到期窗口、权限和数据范围门禁通过。
- 失败去重、敏感信息脱敏、人工换新后可重试通过。
- 自动续期不清除极风验证状态，不改变任务启停和下次计划时间。
- Django 检查、迁移漂移、前后端测试、生产构建和受保护 CI 全部通过。

## 回滚

- 应用回滚到 `v2.44.123-deployed` 对应镜像，并保持自动续期开关关闭。
- 迁移表仅记录去重状态；如必须回退数据库，先停止 Celery Beat，再反向执行 `integrations.0029`。
