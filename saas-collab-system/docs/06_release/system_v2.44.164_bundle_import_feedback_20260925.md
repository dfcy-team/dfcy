# V2.44.164 组合商品导入反馈

## 版本登记

- 登记日期：2026-09-25（Asia/Shanghai）
- 状态：`REGISTERED_FOR_CONTROLLED_RELEASE`；本文件为候选登记，部署成功后再核对并登记虚拟机双账本
- 发布通道：虚拟机功能发布通道
- 前序版本：`V2.44.163`，虚拟机控制账本、统一版本账本、共享版本账本与 `v2.44.163-deployed` 标签均指向 `2054ccf6e54e2c6e88125146fc036d47b8354d93`
- 候选分支：`codex/bundle-import-category-spec`
- 最终发布 SHA：以受保护 `main` 的实际合并提交为准，不能使用候选分支 SHA 代替
- 数据库迁移、菜单权限和路由变更：无

## 变更范围

1. 组合商品导入显示读取 CSV、查找组成 SKU、逐行创建、缓存图片和刷新列表等阶段，以及已处理行数和成功、失败计数。
2. 将服务端字段级校验详情显示在对应 CSV 行；找不到组成 SKU 时提示核对当前租户的普通商品及新旧 SKU 编码。
3. 结果弹窗增加错误明细 CSV 下载，包含源行号、失败阶段、旧 SPU/SKU 编码、组合商品名称和错误原因；图片缓存失败单独记录。
4. 仅在实际生成 BigSeller 新建表时显示该结果提示，旧档案导入不误报。

## 候选验收与发布边界

- 前端针对性测试：3 个文件、13 项通过；前端生产构建通过；`git diff --check` 通过。
- 正式发布须通过受保护 PR 与完整 CI；按 `Developer A Production Release` 工作流从实际主干合并 SHA 构建不可变镜像，完成备份、部署和健康检查。
- 部署后核对虚拟机运行镜像、控制账本及统一/共享版本账本同指实际合并 SHA，再创建 `v2.44.164-deployed` 标签并登记双账本。部署流水线成功本身不等于业务侧导入验收。
