# V2.44.152 商品成本按仓库及所在国家区分——候选登记

## 版本信息

- 登记日期：2026-09-23（Asia/Shanghai）
- 状态：`PR_CANDIDATE`。已重放至 V2.44.151 主干，受保护 PR #197 待 CI；未合并、未部署虚拟机，未记入生产账本，也没有 `v2.44.152-deployed` 标签。
- 编号依据：V2.44.150 已作为商品成本新旧 SKU 导入候选登记；V2.44.151 已由库存工作台候选占用。因此本项使用下一空闲编号 V2.44.152，不复用 151。
- 候选分支：`codex/v244152-warehouse-cost`；重放后功能提交：`dc86517`（原始功能提交 `bd63e5382685e43a1355fc87689985087ebec739`）。
- 开发基线：已部署 V2.44.151 主干 `3a1373bb4b6ddbba3bc48750c99e071659e7b660`；正式版本 SHA 以受保护 PR 的最终合并提交为准。
- 菜单名称保持“商品成本”，不因仓库/国家维度更名。

## 本版范围

1. 成本版本以租户、SKU、**具体仓库**和生效时段为维度；同一 SKU 在同一时期可以因仓库不同而有不同成本。新增版本保留历史，不直接覆盖旧金额。原有无仓库记录保留为历史数据，不自动分派到新仓库。
2. 人工维护、系统生成回填、按时点查询和每期导入均须明确仓库；导入模板增加必填“仓库编码”，继续支持“SKU 编码/旧 SKU 编码二选一”。预览与异常反馈按 SKU＋仓库定位。
3. 商品成本页面显示仓库及所在国家，支持仓库筛选和成本变化历史；系统回填必须先选择仓库。成本查询在指定店铺时校验**仓库所在国家与店铺所在国家一致**，不以国家代替仓库选择。
4. 样品履约的 SKU 成本快照必须指定具体仓库并校验店铺国家一致，按该仓库和业务发生时点取成本；已有快照及历史版本不得被新成本覆盖。
5. 数据库新增 `ProductCostVersion.warehouse` 与 `SampleItem.warehouse`；PostgreSQL 已确认成本的时段互斥约束拆为“同仓库”和“历史无仓库”两组，并增加按仓库/时点查询索引。

## 本地验证

- 商品成本与导入、样品成本快照定向后端测试：26 项通过。
- 样品履约与创建边界后端测试：141 项通过。
- 商品成本前端定向测试：10 项通过；原候选菜单/路由权限快照检查通过；V2.44.151 基线为 108 项菜单、141 项路由，V2.44.152 不新增菜单或路由；前端生产构建通过。
- `makemigrations --check --dry-run`：`No changes detected`。
- 虚拟机部署前只读核对：数据库为 MySQL，`ProductCostVersion` 当前 0 条、`SampleItem` 当前 9,074 条。PostgreSQL 排斥约束 DDL 在 MySQL 分支跳过；成本写入路径通过 SKU 行锁和事务检查同仓库时段冲突。部署后仍须确认两项迁移已应用、样品旧记录保留及待迁移数为 0。
- 上述为本地验证，不替代受保护 PR、目标数据库迁移及虚拟机业务验收。

## 发布顺序与验收边界

V2.44.150 与库存工作台 V2.44.151 已按顺序完成虚拟机部署、双账本登记和远端标签。本候选已重放至 V2.44.151 主干；发布前须通过受保护 PR/CI，校验迁移依赖和 PostgreSQL 时段互斥约束、旧无仓库数据兼容、租户/仓库权限、同国多仓独立成本、跨国店铺拒绝、每期模板与异常导出、历史成本快照不变，并完成备份及回退方案。

本文件仅为候选版本登记，不是上线或部署成功证明。只有实际虚拟机部署、镜像摘要/迁移/健康/业务验收均通过后，发布通道才可创建 deployed 标签并写入生产双账本。
