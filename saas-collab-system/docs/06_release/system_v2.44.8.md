# SaaS 协同系统 V2.44.8 变更登记

## 1. 版本身份

- 登记日期：2026-08-13
- 版本号：V2.44.8
- 来源版本：V2.44.7
- 来源提交：`76ba2c151efa59c4a3a8f153e2d9ca04fd0f8a0e`
- 登记状态：已登记并部署制品
- 运行环境：Pilot（应用层 `192.168.174.131`，数据库层 `192.168.174.132`）

本登记是 V2.44.7 基线上的受控增量。后续构建、部署和回退必须引用本登记及其对应的审核提交，不得回到旧版本或使用未登记的候选镜像覆盖当前版本。

已登记制品：

- 前端：`saas-collab-frontend:v2.44.8@sha256:15a9fb4452afec69801457d1e12a64fddf52bcea0913253e02661dcf3fe60b74`
- 后端：`saas-collab-backend:v2.44.8@sha256:070722258a836db97aab598e1bc402cad72ea34b235d141b3ee51d61a0401e47`

## 2. 授权变更范围

本版本仅登记以下增量：

1. 基础档案新增“国家信息”。
2. 基础档案新增“平台商品明细数据”。
3. 修复相关表格显示问题。
4. 修复前端 API base URL 解析和请求拼接问题。
5. 店铺平台下拉选项改为从平台主数据动态读取。
6. 后端将 LAZADA、TEMU 作为独立平台类型登记和处理。

除上述范围外，不得合并其他模块的业务代码、路由、权限或数据迁移。

## 3. 菜单和路由基线

V2.44.7 的一级菜单顺序和层级保持不变：

`工作台 → 产品开发 → 全球刊登 → 经营分析 → 经营决策 → 达人管理 → 流程协同 → 业务协同 → RPA协同 → API数据接入 → 财务中心 → 报表中心 → 基础档案 → 系统治理 → 治理与试点`

本版本不得重排、重命名、删除或覆盖上述菜单。新增入口仅限“基础档案”授权范围内的“国家信息”和“平台商品明细数据”；全球刊登及其他一级菜单必须继续使用 V2.44.7 基线结构。

## 4. 文件和数据边界

- 前端：国家信息、平台商品明细数据页面及其 API 接线；表格显示和 base URL 修复。
- 后端：国家信息、平台商品明细数据接口和权限；LAZADA/TEMU 独立平台类型。
- 数据迁移：仅执行本版本登记对应的新增迁移，执行前须备份并记录迁移结果。
- 菜单/router：只允许为上述两个基础档案入口提供必要接线，不得改变一级菜单结构或其他模块路由。
- 发布制品：前后端镜像必须使用审核后不可变的 `@sha256` 引用；构建完成后回填实际审核提交和镜像摘要。

## 5. 验证和发布门禁

发布前必须完成并记录：

- 基础档案国家信息、平台商品明细数据的 API、权限和页面定向测试；
- 表格显示、API base URL、店铺平台下拉及 LAZADA/TEMU 类型回归测试；
- 前端生产构建、后端 Django system check、迁移检查和权限同步检查；
- 一级菜单顺序与 V2.44.7 截图/登记比对，确认无非授权菜单变化；
- Pilot 部署前数据库备份、制品清单校验和 HTTPS 健康检查。

未完成上述门禁前，版本状态保持“已登记，待审核提交和制品构建”，不得标记为已发布。

## 6. 回退策略

出现菜单、权限、迁移或数据异常时，停止发布并回退到已验证的 V2.44.7（提交 `76ba2c151efa59c4a3a8f153e2d9ca04fd0f8a0e`）及其匹配数据库迁移状态。回退操作须保留备份、操作人、时间和验证结果。
