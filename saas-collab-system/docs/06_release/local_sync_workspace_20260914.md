# 同步任务与运行记录本地调整

日期：2026-09-14。状态：本地未发布增量；未提交、推送或部署虚拟机。

## 变更

- 复用既有同步任务、运行记录及详情路由，不修改菜单注册或权限路由快照。
- 任务页增加创建向导、独立缺失预览、多选平台和主体筛选、分页、最近结果与配置跳转。
- 创建向导先读取候选和重复任务，再检查条件；确认创建才 POST，默认手动、停用，不执行。数据库既有唯一约束继续防止重复任务。
- 移除任务页内嵌异常处置表和抽屉，保留数量及既有同步异常入口。
- 运行记录独立展示执行事实、筛选、详情与返回任务；真实运行仍返回任务页确认，模拟重试创建新记录且继续受后端校验。
- 时间标注 UTC；未采集数量和未知执行类型不补零或推断；内部异常包装转换为可读提示。
- 后续执行记录保存实际真实／模拟类型。真实成功统计排除模拟和类型不明的历史运行。

## 验证

- 相关后端隔离测试：55 项通过（SQLite 内存数据库、禁止网络）。覆盖创建不执行、重复创建、停用、真实／模拟隔离、跨租户筛选等。
- 相关前端测试：7 个文件、38 项通过。覆盖创建确认与防双击、预览不创建、阻塞、分页筛选和错误展示。
- 前端构建通过；菜单权限快照仍为 100 项菜单、133 项路由。
- 全量前端测试发现未修改模块中的竞品 Mock 创建和商品图片 CRLF 断言失败，未扩展修改这些模块。
- 本地真实浏览器进入验收时登录已过期，需要用户重新登录后完成页面实测。没有为验收执行真实平台同步或写入测试任务。

## 未采集数据的边界

- 不添加缺乏后台事实的排队、部分成功或百分比进度。
- 未记录的触发方式、失败阶段、历史同步范围及数量显示“—”。
- 尚未增加自动刷新；手动刷新保留筛选和分页，且不会重新触发任务。
- 本轮未修改虚拟机菜单、数据、版本或部署。
