# 本地缺失同步任务整改

范围：本地 3001/8002；未发布虚拟机或阿里云，未调用真实平台接口，未启动同步或调度器。

## 预览修正

缺失任务预览原来遍历全部授权，将同一仓库两条已撤销历史授权也当作待配置项。对店铺及仓库查询分别排除 `revoked`；不删除历史记录，不隐藏当前 pending/expired/error 授权的问题，不放宽租户、数据范围及执行门禁。

新增测试先复现重复预览，再修正。选定回归集（增量预览、同步能力门禁、仓库绑定流程）27 项通过，129.73 秒；测试使用隔离内存数据库，不访问真实平台。Django system check 通过。

## 用户授权范围内的本地配置

- 确认用户刷新后的 Shopee 授权有效；没有再次刷新或重新兑换 Token。
- 通过用户重新登录的正常页面会话，为 Shopee 授权 1、TikTok 授权 2 保存 PRODUCT、ORDER、RETURN_REFUND 读取能力，人工模式、来源优先级 100、平台写入关闭；其他能力保持关闭。
- TikTok 已返回的 scopes 包含对应三类能力。Shopee 授权记录没有声明 scopes；本地读取开关不是平台权限或接口连通性证明，正式执行仍须通过现有门禁及平台响应验证。
- 创建七条默认停用、人工模式任务，无下次调度时间。

| 主体 | 商品 | 订单 | 退货退款 | 库存 |
| --- | --- | --- | --- | --- |
| TikTok 授权 2 | 6 | 5 | 7 | — |
| Shopee 授权 1 | 9 | 8 | 10 | — |
| 极风仓库授权 7 | — | — | — | 11 |

## 验证与边界

七条任务的本地主体门禁检查通过；两个店铺六项读取能力已落库，写入均关闭。运行记录仍为 0，仓库历史授权 5、6 均保留为 revoked。

确认没有待处理 OAuth 或运行中同步后，重启本地后端。未登录受保护 API 返回 401；页面重新预览显示当前权限范围内没有缺失任务，健康摘要为任务总数 7、能力阻塞 0、启用任务 0。

本次不证明七类真实同步已经连通或完成数据入库。任务尚未启用，未采集平台数据；当前令牌有效性及门禁仍需在实际执行时重新检查。
