# 本地 OAuth 脱敏诊断整改记录

日期：2026-09-10。范围仅限本地；未发布、同步到虚拟机或阿里云。

## 环境与数据保护

- 代码基线：338085edb3d163a09987291d4cc0a5adf89d933d，保留已有未提交改动。
- 前端：http://127.0.0.1:3001；后端：http://127.0.0.1:8002；DEBUG=False。
- 联调库：saas_collab_live_local_20260909，Docker MySQL 127.0.0.1:3307。
- 独立加密托管：https://127.0.0.1:8444。未修改已审批的网络、托管或开发者凭据配置。
- 新增迁移 integrations.0026_oauth_configuration_digest 已在上述本地联调库应用；仅增加授权会话的 SHA-256 配置摘要字段，不重建业务表。
- 迁移前已完成本地数据库备份并检查 gzip 与 mysqldump 完成标记：
  C:/Users/Administrator/Desktop/开发/.codex-tmp/live-local-20260909/oauth-before-0026-20260910-103609.sql.gz
  压缩大小 26,580,549 字节；位于既有受限目录，不应提交或对外传送。

## 本次改动

1. 授权失败增加闭合字段集诊断：诊断编号、阶段、错误类别、平台、配置 ID、授权会话 ID；可用时记录 HTTP 状态、精确白名单错误码及 SHA-256 请求标识摘要。
2. 不记录原始异常文本、原始平台响应、完整请求/回调地址、授权码、签名或任何凭据。未知平台错误显示“未分类平台错误”；不从未知错误推断密钥或签名原因。
3. 区分读取开发者密钥、兑换 Token、保存 Token、店铺核验、授权落库，以及回调/权限校验阶段。
4. Shopee、Lazada、TikTok 的授权码兑换禁用网络自动重试；本地登录 401 不再自动重放手动回调。普通请求继续使用既有登录续期逻辑。
5. 仅比较根路径时将空路径与 / 视为一致；保留其他路径、域名、端口、参数和片段规则。
6. 授权开始时保存配置摘要；兑换前和授权落库前重新核对配置与凭据版本。旧会话无摘要或配置已变化时要求重新发起授权，不补猜历史版本。
7. 保留一次性 state、用户/租户/店铺绑定及权限范围校验；权限失效在兑换前即拒绝。
8. 授权记录与成功审计在同一事务中保存。重新授权的旧托管引用仅在事务提交后清理；事务失败只撤销新引用，不清除旧授权。
9. Shopee 店铺核验不再用回调 shop_id 补齐缺失的响应身份证据；必须取得明确且匹配的店铺 ID。
10. 页面沿用现有组件与样式（impeccable 澄清反馈约定），持续展示阶段、诊断编号和处理提示。超时、网络中断、服务异常明确标注结果尚不能确认；不显示虚假成功。

## 合成验收

所有后端用例使用隔离 SQLite 内存库与禁止外网连接的运行器；没有调用真实平台 API。

- 后端：74 passed，3 skipped，131.36 秒。
- 随后补充“店铺查询参数拒绝不是身份不匹配”分类用例；诊断单元组再次运行 14 passed，0 failed。
- 跳过项：3 个 POSIX 文件权限检查，Windows 不适用；不计为通过。
- 前端：28 passed，0 failed。
- 前端生产构建成功；菜单权限快照一致（100 项菜单、133 项路由）。
- Django makemigrations integrations --check --dry-run：无遗漏模型变更。
- 针对本次文件的 git diff --check：无空白错误。
- 运行态核对：DEBUG=False；新迁移存在；3001 的已登录页面可以打开验收专用 Shopee 店铺授权弹窗，授权按钮就绪。

后端覆盖文件：

- tests/test_oauth_safe_diagnostics.py
- tests/test_oauth_diagnostic_transactions.py
- tests/test_marketplace_callback_acceptance.py
- tests/test_oauth_callback_target_security.py
- tests/test_lazada_store_authorization.py
- tests/test_http_custody_legacy_replacement.py
- tests/test_custody_service.py
- tests/test_custody_security_gate.py
- tests/test_shopee_production_oauth_readiness.py
- tests/test_tiktok_callback_configuration.py

前端覆盖文件：

- tests/request-auth-retry.spec.js
- tests/subject-api-access-dialog-runtime.spec.js

已验证：正常兑换、平台认证拒绝/参数错误/未知错误、托管认证失败、兑换超时与 503 单次发送、Token 保存失败/超时、身份缺失或不匹配、数据库回滚、原授权保留、重复回调、自动成功后的手动重放、登录失效、配置/凭据版本变更、根路径差异和未脱敏文本不进入诊断。

## 真实复测：已取得 HTTP 403，授权未完成

本次已打开本地“验收专用 Shopee 店铺 → API 接入”，选中“验收专用 Shopee 接入”；用户完成一次新授权回调并反馈诊断编号。没有提交旧回调，也没有再次兑换本次授权码。

从本地安全审计读取到：

| 字段 | 结果 |
| --- | --- |
| 诊断编号 | 167ba392109d4b34b1d92c414669d17c |
| 授权会话 / 配置 | 14 / 5 |
| 阶段 | exchange_token（Token 兑换） |
| HTTP 状态 | 403 |
| 分类 | authentication_rejected（上游拒绝） |
| 平台错误码 | UNCLASSIFIED（未命中精确白名单，不记录原文） |
| 脱敏请求标识 | req-7435c701449d6685 |
| 会话状态 | failed；不得重复提交该回调 |
| 验收店铺授权记录数 | 0；未进入 Token 保存及授权落库 |

只读核对：生产环境、API 主机 partner.shopeemobile.com、Token 路径 /api/v2/auth/token/get；Partner ID 为数值且与所选配置一致；本地托管可读取非空密钥，无首尾空白；配置版本 7、凭据版本 6。未打印或修改密钥。本机 UTC 与工具 UTC 核对未见明显时钟偏差。

这些结果只能确认上游在兑换阶段返回 403，不能证明密钥正确，也不能判定一定是签名错误、应用生产权限或其他网关限制。官方文档读取亦受访问限制，未用第三方推测替代本次错误依据。需要核对 Shopee 开发者后台的应用状态、生产权限以及应用与店铺授权条件；未经进一步确认不进行第二次真实兑换。

未改变 Partner ID/Key、签名算法、平台环境或审批策略。整改的合成验收通过；真实授权验收仍未通过，等待进一步平台侧证据。

## 回退与限制

- 回退只针对本次代码改动；保留已有修改。优先保留新增摘要字段，不删除授权会话、迁移历史或恢复整库。
- 若需数据库恢复，必须先停止本地写入、备份恢复前状态并另行确认，避免覆盖迁移后的业务数据。
- 旧引用提交后清理失败会留下失败审计，不将已提交的新授权改成失败或撤销；需由管理员据审计核对托管清理。
- 本次未执行订单/库存同步、真实 TikTok/WMS 授权、生产发布或全站视觉矩阵验收。

## 追加调查：来源 IP 白名单（2026-09-10）

用户的新截图对应诊断 46768847df7f48cdb86c1ddd27654ba4，授权会话 15、配置 5；仍为兑换阶段 HTTP 403。

为区分请求格式与来源限制，执行了 10 次不含授权码、不含 shop_id 的最小负向请求；没有重放真实回调或取得 Token。默认请求与携带真实客户端 User-Agent/Accept 的请求均返回 403，因此未将增加请求头作为修复提交。

检查仅输出白名单标识和布尔分类，不保留响应全文。平台错误字段包含 IP 标识，message 含 whitelist；响应报告的公网地址为 23.186.200.163。这指向当前请求的来源 IP 白名单检查，不能用修改本地 DEBUG、关闭网络门禁或更换授权码解决。

本地补充：

- 仅对 Shopee 的 HTTP 403，且错误字段具有独立 IP 标识、说明包含 whitelist/allowlist 时，增加固定类别 ip_allowlist_rejected。
- 未知原始错误码仍保持 UNCLASSIFIED，不回显 message、地址或原始错误文本。
- 页面按既有后台样式提示检查 Shopee 开发者后台当前应用的来源 IP 白名单；继续保留诊断编号和禁止重放提示。
- 新增失败复现用例先失败再修复；后端诊断组 19 passed，前端相关组 29 passed。
- 一次性诊断脚本已移除，避免再次执行负向请求。未改动 Shopee 后台白名单、代理出口、虚拟机或阿里云配置。

尚未完成的外部步骤：由应用管理员核对并按安全策略放行实际固定出口。23.186.200.163 是本次响应报告地址；代理或网络出口变化后不能假设仍是该地址。保留已有白名单项，不开放全网。白名单确认前不继续真实授权。

## 白名单更新后的单次验证（2026-09-10）

用户提供 Shopee 后台截图：IP 白名单已启用，新增 23.186.200.163，原地址保留。随后仅执行一次不含授权码、不含 shop_id 的签名负向请求；沿用本地配置 5、托管凭据、TLS 校验和受控出口，不自动重试。

- HTTP 200，平台错误码 error_param。
- 脱敏请求标识 req-b318d5b3461a51b0。
- 未发送授权码，未兑换或保存 Token，未更改业务授权。

本次请求已从 IP 拒绝进入参数校验阶段；缺少授权参数是此次诊断的预期结果，不计为授权成功。下一步由用户发起一次全新 Shopee 授权；旧失败回调不得重放。Token 安全保存、店铺身份核验及授权落库仍待验收。没有向虚拟机或阿里云发布。

## 新回调：店铺身份证据不足（2026-09-10）

用户新回调对应审计 53、会话 17，诊断 34bc3ae454334ab79794bfb3dc006ba0：verify_store / identity_rejected。验收店铺授权记录仍为 0。没有重放此回调，也没有再次调用平台接口。

现有代码对店铺信息响应缺少 shop_id 和返回不同 shop_id 使用同一错误，身份比较失败未附带 HTTP 状态和脱敏请求标识。历史响应没有保留，不能据此声称确定属于哪一种；官方接口文档本次读取返回 403，未用第三方实现作为放宽核验的依据。

本次仅修复诊断信息缺失：

- 身份证据区分 shop_id_missing、shop_id_mismatch、invalid_shop_response，并带入安全响应元数据。
- 失败时把 Token 响应 shop_id_list 与目标主体的比较仅转为固定枚举：包含、不包含、未提供、结构异常。不会输出列表或实际值；不作为接受授权的新条件。
- 闭集过滤新增字段，未知文本不进入响应或日志。
- 不改变身份核验规则、失败后新托管引用清理、state 消费及旧授权保留规则；无数据库迁移。

新增诊断用例先复现 7 项失败，修改后诊断单元组 25 passed；连同授权事务、回调验收和回调目标安全组共 57 passed（125.80 秒，隔离 SQLite、禁止外网）。仅重启本地 8002 联调后端加载诊断补丁，3001 地址不变。真实授权仍待新的单次回调证据，不能计为修复通过。

## 身份核验误判修正（2026-09-10）

新诊断 b293a389406a46d5a2fdd59bdf244874（审计 55、会话 18）提供了此前缺失的依据：verify_store / identity_rejected；HTTP 200；identity_evidence=shop_id_missing；token_scope_evidence=contains_callback_shop；脱敏请求标识 req-307999a2c74fdf38。平台 Token 响应中的 shop_id_list 包含回调主体，而店铺信息响应不含 shop_id。本地仅接受店铺信息显式 shop_id 的规则导致本次拒绝，不是档案编码填写错误。

修正范围仅 Shopee 本地连接器：

- 由本次 Token 兑换响应的 shop_id_list 核对回调主体，不能由前端传入的列表替代。
- 店铺信息没有 shop_id 时，必须同时满足授权范围匹配、已签名的店铺只读请求成功，以及非空的 shop_name、region 文本；空响应、仅请求标识等不得通过。
- 店铺信息明确给出不同 ID、Token 授权列表不包含目标主体、列表结构错误时继续拒绝。未提供授权列表时，仍要求店铺信息显式 ID 匹配。
- 不改变当前用户/租户/店铺绑定检查、state 一次性消费、失败后引用清理或已有授权保护。未手填店铺 ID、未重放会话 18、未调用新的平台请求。

对应复现及负向测试修改前 12 failed / 29 passed，修改后诊断组 41 passed。真实成功仍需一次新的授权验证 Token 托管、身份核验与授权落库，不能仅以单元测试计为平台联调通过。无数据库迁移、无虚拟机或阿里云发布。

回归完成：诊断、授权事务、回调验收、回调目标安全、Shopee 就绪检查共 76 passed（132.44 秒，隔离 SQLite、禁止外网）。确认配置 5 没有未过期 pending 会话后，仅重启本地 8002 后端加载修复；前端地址仍为 3001。
