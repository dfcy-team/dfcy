# 本地增量登记：同步定时设置（2026-09-14，未发布）

范围仅本地。未提交、推送或部署；菜单和路由结构保持不变；未启用任务、未启动调度服务或真实同步。

## 修改

- 任务详情增加手动、间隔、每日、每周设置，时区、未来三次预览、暂停至、漏跑跳过/补跑一次。Cron 拒绝新增配置并停止派发。
- 保存不执行、不改变启停状态；运行或排队期间不能修改；只修改暂停/漏跑策略不重置原计划时点。
- 计划时点唯一记录、持久化派发声明和消费端重复检查；每次执行保留授权与只读准入校验。手动执行及失败重试不改变正常计划。
- 运行记录补充计划时间、触发来源、计划快照、排队耗时；跳过、派发失败和未执行与实际运行区分。
- 调度心跳只反映派发服务，不表示消费者或平台成功。没有观测记录时显示未知。
- 按 Impeccable 的产品界面规范，将计划设置保留在现有详情中，明确说明保存、启用和执行的区别。

## 验证

- 相关后端回归 65 项通过（隔离 SQLite、禁止网络访问）。
- 前端运行时及同步展示回归 29 项通过。
- 生产构建通过；菜单权限快照一致：100 菜单、133 路由。
- integrations 模型迁移一致性检查通过；0027 已应用至本地数据库，仅新增计划派发表和心跳表。

## 尚需实际环境验收

- 未进行已登录浏览器交互验收、持续调度及真实平台执行验收。
- 漏跑容差为 60 秒，与现有分钟级派发周期对应。
- 队列提交结果不确定时不自动重复派发同一时点。工作进程在创建运行前中断而留下排队/处理中记录，需要人工核对队列后处理，避免盲目重发。
- 隔离测试验证逻辑和重入，尚未通过生产数据库多进程压力测试证明并发行为。
