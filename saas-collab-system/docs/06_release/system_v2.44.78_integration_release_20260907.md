# V2.44.78 集成发布记录

## 发布基线与边界

- 日期：2026-09-07（Asia/Shanghai）。
- 目标：检查开发 A、开发 B 的新增修改，与 V2.44.78 基础档案候选一起集成、登记、部署虚拟机并验证。
- 基线：V2.44.77，`4a43e3eb0b8d121e102525d1b2b2bb744ba5f749`。
- 当前文档状态：集成阶段；本提交本身不证明生产部署成功，部署结论以工作流及运行时证据为准。
- 从干净 V2.44.77 基线迁入基础档案候选净增量；不整体合并旧的脏工作区。
- 不调整菜单名称、顺序、前端路由、权限目录、数据库模型或迁移、Compose 和发布工作流。

## 开发 A、B 检查

- 开发 A：PR #89 的头提交仍为 `3689d5233de4ff2edf190218f8d8560524ba57c9`；所涉既有实现已包含在发布基线，没有发现新的已提交代码增量。
- 开发 B：PR #86 的头提交仍为 `0a5ec334b9b1d28c0f982afd369ce7935ec74e25`；所涉实现已包含在 V2.44.77，没有发现新的已提交代码增量。
- 开发 B 新提供的达人数据包不属于此次代码集成。现有只读检查发现重复标识、必填数据缺失及导入脚本缺失等问题，本次不导入、不清理生产业务数据。

## V2.44.78 功能增量

1. 分类背景色刷新与层级匹配；图片列前置及点击放大。
2. SPU、SKU 删除引用保护和手动状态维护。
3. 历史商品导入扩展为 20 字段，支持自动、新增、更新模式；新旧 SKU 精确匹配、空字段不覆盖、逐行事务及冲突反馈。
4. SKU 多颜色、多规格组合生成，最多 200 个组合，去重并跳过已存在组合，失败事务回滚，兼容原单个生成接口。
5. 图片批量缓存状态、结果和失败重试；完成后保存刷新，刷新失败保留结果。
6. 平台商品明细关联内部 SKU 图片，维持租户隔离与关联预加载。

既有分类移动及属性编码修复已随前序版本发布，本次应保留，不重复覆盖为旧版本实现。

## 验证与登记要求

- 在集成候选上重新执行专项测试、Django 检查、迁移检查、前端测试与构建，再执行发布 CI。
- 生产发布使用不可变镜像及 V2.44.77 当前 Redis digest；不在虚拟机本地构建源码。
- 发布后检查容器健康、镜像摘要、提交、接口只读冒烟及菜单注册表不变性。
- 发现两份旧共享版本账本仍停留在 V2.44.74，而 CI 权威账本及生产运行已为 V2.44.77。按不可变发布证据补齐 V2.44.75–77，再登记实际部署成功的 V2.44.78；登记应加锁、备份并支持幂等重跑。
- 不将本地模拟 API 浏览器测试表述为生产登录后的页面验收。

## 集成候选验证结果

- 发布基线静态检查：`PRODUCTION_BASELINE_CI=PASS`。
- 前端定向回归：8 个文件、33 项通过。
- 前端完整测试：80/81 个文件、480/481 项通过。唯一失败为未改动的 `development-competitor.spec.js` 第 56 行，模拟创建需求返回 `data=null`，读取 `requirement.data.id` 失败。主代理在 V2.44.77 候选目录独立执行 `npm test -- --run tests/development-competitor.spec.js`，同样为 4 项通过、1 项失败，确认属于既有基线问题；未修改或跳过该用例。
- `npm run permissions:check`：通过，100 项菜单、133 项路由。
- `npm run build`：通过。
- 后端专项回归：36 项通过；`manage.py check` 无问题，`makemigrations --check --dry-run` 无变更，相关模块编译检查通过。
- 主审发现并修正 PostgreSQL 可空关联行锁问题：限定主表锁定，避免在外连接可空侧应用 `FOR UPDATE`；自定义数据范围的 `DISTINCT` 查询先解析可见主键，再锁定主表。测试运行在本地 SQLite，不将其表述为真实 PostgreSQL 集成测试。
- 补强异常历史数据的租户边界，防止跨租户生成关联参与商品图片及明细更新。
- 基础字典页与商品序列化文件和候选源无功能差异；编码服务仅有空行差异，保留 V2.44.77 基线，无重复覆盖。
