# V2.44.103 角色权限配置反馈修复

日期：2026-09-14。源码基线：主干 V2.44.101，`56e7e2d88b7d994769021605c1c5b9c3002a8c6b`。

## 版本登记

- 候选版本：`V2.44.103`
- 候选分支：`release/v2.44.103-role-permission-feedback`
- 功能提交：`5471f774f360c39beb3b86eb665583d49c3267fa`
- 发布通道：虚拟机功能发布通道
- 当前状态：已登记、已移交、待发布通道执行基线核对和部署
- 数据库迁移：无
- 菜单、路由、权限目录：无新增，快照仍为 101 项菜单、133 项路由

`V2.44.102` 已由达人管理刷新性能优化候选占用，本版本不重用该版本号，也不默认捎带尚未合入主干的 V2.44.102 增量。发布通道如先合入 V2.44.102，必须将本候选重放到最新受保护主干后再部署。

## 修复内容

1. 角色高级配置的每个权限分组显示“已选 X / 总数”，勾选或取消后实时更新。
2. 保存失败时解析并显示后端字段校验详情，例如业务范围、仓库或权限编码错误，不再只显示通用的“提交内容校验失败”。
3. 业务范围选项请求增加并发序列保护，旧租户请求不能覆盖当前租户选项。
4. 范围选项加载或失败时禁止提交自定义范围，并提供显式重试，避免用旧选项或未完整选项保存。

本候选不修改角色 36 或其他角色的现有权限和数据范围，不新增权限码，不放宽租户隔离或服务端鉴权。

## 验证证据

- 角色权限、数据范围和权限树定向回归：5 个文件、21 项通过。
- 新增反馈用例：3 项通过，覆盖已选计数、字段错误翻译和租户请求防串线接入。
- 前端生产构建与权限快照检查：通过。
- Playwright 真实浏览器：首个分组由“已选 0 / 14”实时更新为“已选 1 / 14”，控制台 0 错误，折叠箭头和布局正常。
- 主干全量前端回归（`VITE_USE_MOCK=true`）：644 项通过，1 项基线既有失败。失败为 `product-detail-image-batch.spec.js` 对商品图片批处理让出事件循环的静态契约，本候选未修改该模块或用例。发布通道应在最终主干上重新执行全量门禁，不得将该基线失败记为本修复通过。

## 部署与回退

发布通道应先刷新虚拟机当前受管版本和主干状态，将候选重放到当时最新主干，通过 CI、生产构建和发布前健康检查后，再使用虚拟机功能发布通道部署。不需要数据库迁移或数据修复。

若角色权限页加载、计数、保存或租户范围出现回归，回退到部署前的上一个受管镜像。本版本无数据库变更，应用回退不需要反向迁移。

## 虚拟机验收

1. 打开自定义角色的高级配置，每个菜单分组应显示已选数和总数。
2. 勾选或取消一项菜单权限，对应分组计数立即增减。
3. 保存正常配置应成功；提交不属于当前租户的范围 ID 时，页面应显示具体字段错误。
4. 快速切换目标租户并打开角色，业务范围选项不得出现上一租户数据。
5. 发布后核对角色 36 的权限数和数据范围与发布前一致，验收过程不额外赋权。
