# 达人管理菜单合并记录

## 当前状态

- 状态：已完成并关闭
- 完成日期：2026-08-10
- 应用层版本：`saas-collab-frontend:creator-v2.44.2-20260810`
- 菜单位置：`经营决策`之后、`流程协同`之前
- 菜单顺序：`经营决策 → 达人管理 → 流程协同`
- 菜单配置文件：`frontend/src/router/menu.js`

本次调整只改变一级菜单的排列位置，没有修改达人管理的路由、权限、子菜单，也没有调整其他模块的结构。

## 后续模块合并规则

1. 新模块合并前，以 `frontend/src/router/menu.js` 的当前顺序为基线，不得使用旧版本菜单文件整体覆盖。
2. 达人管理一级菜单必须保留在经营决策与流程协同之间。
3. 新模块只插入自身菜单块，不得重排、删除或重建已有一级菜单。
4. 保留达人管理菜单的 `internal`、`permissions` 和全部 `children` 配置。
5. 生产构建必须设置 `VITE_USE_MOCK=false`，避免加载模拟账号及旧菜单权限。
6. 合并后至少验证：经营决策、达人管理、流程协同三者顺序，V2.43 原有菜单完整性，以及角色 `002 达人管理` 的菜单可见性。

## 达人管理菜单内容

- 建联任务
- 送样履约
- 视频结果
- BD绩效
- 店铺订单
- 品牌达人资源库

