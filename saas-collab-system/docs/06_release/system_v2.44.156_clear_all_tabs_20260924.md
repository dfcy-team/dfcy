# V2.44.156 页签一键清空与上限处理

## 版本登记

- 登记日期：2026-09-24（Asia/Shanghai）
- 状态：`REGISTERED_FOR_CONTROLLED_RELEASE`；仅为候选登记，尚未合并、部署或写入生产账本
- 发布通道：虚拟机功能发布通道
- 前序版本：`V2.44.155`，远端标签 `v2.44.155-deployed`、主干提交 `f91803ab25a067e47bcb987eb37a02f03012155c`
- 候选分支：`codex/tab-clear-all`
- 功能提交：`6436813`（最终发布 SHA 以受保护主干合并提交为准）
- 数据库迁移、菜单权限与路由变更：无

## 变更范围

1. 顶部页签栏在存在可关闭页签时显示“清空页签”，一键关闭所有非工作台页签并回到工作台；工作台始终保留。
2. 达到用户设置的页签上限后，尝试打开新菜单时提示选择：自行关闭指定页签，或清空已有页签后打开目标菜单。
3. 保留逐个关闭和拖拽排序能力；清空后的页签状态继续写入现有会话存储。用户页签数量设置及默认值 15 不变。

## 候选验收口径

- 前端 123 个测试文件、749 项测试通过；前端生产构建及菜单权限快照检查通过（108 项菜单、141 项路由）。
- `git diff --check` 通过。
- 发布通道需复核：非工作台页签可单独关闭；“清空页签”后只保留工作台；达到上限后“自行关闭”不改变当前页签，“清空后打开”仅保留工作台和目标菜单。

## 发布与账本边界

本登记不是部署成功证明。发布前重新核对虚拟机运行提交、不可变镜像、正式双账本与最新 deployed 标签一致，并确认 V2.44.156 未被占用；由虚拟机功能发布通道通过受保护 PR 与 CI、备份和受控部署完成发布。只有镜像、健康及功能验收通过后，才能以实际合并 SHA 创建 `v2.44.156-deployed` 标签并在虚拟机统一账本和共享账本登记。不得将候选 SHA 作为生产 SHA。
