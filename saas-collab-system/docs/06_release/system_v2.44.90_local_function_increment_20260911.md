# System V2.44.90 本地功能增量发布候选

## 版本与边界

- 父版本：V2.44.89，已部署标签 `v2.44.89-deployed`，提交 `cd32a320c8aaa8b632ae6cf232d9acf46d33d0b0`。
- 候选版本：V2.44.90。部署成功前不得登记为已部署或创建 deployed 标签。
- 仅更新现有功能；菜单、路由、权限注册及角色授权不变（100 项菜单、133 项路由）。
- 不发布本地环境配置、数据库、凭据、运行日志或本地验收数据。不覆盖生产配置或启动额外同步任务。

## 功能增量

- 销售总览、门店销售、SKU 销售和经营总览：报表指标、筛选、排序、卡片控制图表、完整日期及单位展示；按店铺时区统一日期筛选和日报归属。
- 销售订单、退款退货、导出与数据质量：字段、状态、详情及查询条件一致性；价格中心明确待接入状态，不提供真实调价。
- 库存分析：库存快照、SKU 自动关联、关联率、排序及缺失趋势提示；销售 SKU 关联和补货事实预览后台能力。
- 店铺与仓库档案：授权回调、凭据绑定、平台身份校验、仓库发现、就绪检查与脱敏诊断。
- API 数据接入：只读准入、连接配置、缺失任务预览与补齐、启停、模拟隔离、运行诊断及错误处理。

## 数据库与回退

- 唯一新增迁移：`integrations.0026_oauth_configuration_digest`，新增允许空值字符串的配置摘要字段，默认空字符串。
- 不执行数据清理或角色权限迁移。现有数据不覆盖；本地 SKU 关联结果不复制到生产数据库。
- 生产迁移仅通过现有受控流程，在备份成功后执行；DDL 锁定耗时取决于现场表规模，由部署超时和门禁控制，不假定无锁。
- 回退到已登记 V2.44.89 应用镜像；新增字段可保留，不手工反向删列。数据库恢复由现有备份流程处理。

## 验证与发布登记

- 本地前端生产构建与菜单注册一致性检查通过。
- 最新日期及空状态专项：后端 17 项、前端 38 项通过；四个本地报表浏览器检查通过。
- 发布前 VM 受控检查：Actions `34596648179` 成功。
- 完整增量回归、PR CI、发布 SHA、镜像摘要和最终部署状态以本候选关联的 PR、Actions 与受控发布账本为准，不提前标记成功。
- 复用上一受控发布的 Redis 摘要；不修改生产控制树、Compose 或环境设置。
