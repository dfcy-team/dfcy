# V2.44.154 组合商品导入与属性编码统一

## 版本登记

- 登记日期：2026-09-24（Asia/Shanghai）
- 状态：`PR_CANDIDATE`；本登记不表示已合并、部署或写入生产发布账本
- 父版本：已部署 `V2.44.153`，远端 `v2.44.153-deployed` 与主干均指向 `99cb821b601e903594832b2038d5ef52b56887af`
- 候选分支：`codex/bundle-import-workflow`
- 数据库结构迁移：无

## 变更范围

1. 商品明细数据中的“组合商品导入”和“旧组合关系迁移”直接打开上传弹窗，不再先展示组合商品列表；操作完成后刷新商品明细。
2. 组合商品导入识别 UTF-8 与 GB18030 CSV，预先校验必需表头，并按原文件物理行号报告错误。按文件中的单品编码分批查询普通 SKU，同时支持新 SKU 编码和旧 SKU 编码；旧编码匹配到多条商品时拒绝自动关联。
3. 组合商品编辑、导入模板、校验提示和 SPU 编码说明统一使用“属性编码”。服务端允许字母或数字属性编码。为保证旧文件可继续导入，解析器内部兼容旧表头“季节编码”；新模板不再输出该词。

## 验证与发布验收

- 附件 `组合商品补充20260923.csv` 只读解析为 291 条数据、67 列；必需表头齐全，291 条的必填字段均非空。该检查不代表 291 条已写入数据库。
- 前端相关测试 25 项通过，生产构建通过；后端 SKU 筛选和组合创建定向测试通过；`git diff --check` 通过。
- 发布通道须从受保护主干最终合并提交构建不可变镜像，完成备份、迁移检查、健康检查和真实租户的导入预览/错误行号/新旧 SKU 映射验收。成功后记录实际合并 SHA、镜像摘要、备份、部署时间和结果到生产发布账本，并创建 `v2.44.154-deployed` 标签。若门禁失败，保持 V2.44.153 运行态并记录失败原因。
