# V2.44.65 模块开关与业务工作台名称增量登记

## 版本登记

- 登记日期：2026-09-04
- 版本：V2.44.65
- 父版本：V2.44.64（`v2.44.64-deployed`）
- 来源分支：`release/v24464-business-workbench-name`
- 模块开关来源提交：`164f417`（模块发布控制）
- 工作台名称来源提交：`fa3b1ec`（业务工作台名称）
- 数据库迁移：无新增迁移；沿用现有配置中心结构，未修改已执行迁移
- 发布方式：受控 CI 构建不可变镜像后部署虚拟机

## 增量范围

- 增加模块状态模型（关闭、仅 Mock、试运行只读、正式启用），支持按环境配置和生产配置读取。
- 增加“模块发布控制”页面、路由和菜单节点，使用既有 `config.system.manage`、`config.view` 权限。
- 登录后的当前用户响应可按参数返回模块状态，前端导航和直接路由按模块状态执行可见性拦截。
- API 数据接入只读同步、全球刊登生产队列增加模块开关校验。
- 业务工作台标题和导航品牌名称统一调整为“业务协同工作台”。
- 保留 V2.44.64 既有菜单顺序、路由、权限目录和达人管理增量；未删除或重排既有菜单。

## 兼容与回滚

- 不改写历史业务数据，不新增数据库字段或不可逆数据操作。
- 未配置模块状态时保持历史全启用兼容行为；管理员可通过模块发布控制页面提交审批版本。
- 如验收失败，回退至 V2.44.64 应用制品即可，无需数据库逆向迁移。

## 发布前后检查

- 必须通过后端 Django 检查、迁移检查、模块开关专项测试和前端构建。
- 必须核对菜单、路由、权限目录相对 V2.44.64 无非预期删除或重排。
- 部署后核对前后端/Celery 镜像摘要、Git 修订号、容器重启次数、健康入口和权限目录完整性。
