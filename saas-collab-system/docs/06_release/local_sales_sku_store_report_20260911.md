# 本地销售 SKU 关联与门店报告

范围：当前本地租户 1；先完成销售事实关联，再调整门店销售页面。未访问云主机，未修改 BigSeller 数据，未调用平台同步或写入 API。

## SKU 关联

- 1,212 条订单商品行关联到 499 个内部 SKU；无退款商品行。
- 1,210 条使用精确编码；2 条仅忽略编码边界换行进行匹配，保留原始字段。
- `HYYLZH392-G-Q` 未找到唯一匹配，保留未关联。
- 审计记录：127、128，动作 `sales_sku_link`。既有库存关联未改动。
- 同租户新编码、旧编码和已生成旧档案的候选取唯一结果；参考同门店平台商品/规格映射，以及关联订单商品行。候选冲突、已有 SPU 不一致、跨租户关系不写入。已有关联不覆盖；不进行大小写或模糊匹配。
- 命令 `link_sales_skus` 默认预览；应用需要预览数量、同租户全范围接入管理权限，并在事务内记录审计。本次为已有销售事实回填，不意味着开启未来销售同步自动关联。
- 写入前后对比订单、退款及商品行事实摘要，仅允许内部 SKU/SPU 关联字段改变；产品和同步运行记录数量不变。
- 修复 SKU 汇总同一键被多组商品行覆盖的问题；订单数按订单去重。多来源商品 ID/名称不一致时不伪装成一个确定值，平台 SKU 保留集合展示，关联后仍支持原编码搜索。

## 门店报告

参考用户已打开的 BigSeller 店铺报告结构，沿用本系统组件和克制后台风格：

- 紧凑的平台、门店、币种、日期筛选与快捷日期。
- 店铺概览：按币种展示订单、非取消销售额、销量、退款及取消指标。
- 真实趋势图，日/周/月、显示图表与区间统计；没有同期数据不生成环比。
- 汇总明细支持服务端全量排序后分页、列选择、门店固定列、只读详情；窄屏取消固定列以便横向阅读。
- 门店名称下展示平台、站点，销售额前置；默认导出类型选择门店销售，仍需用户确认创建。
- 汇总及趋势基于完整筛选范围，不以当前分页代替汇总；币种不换算、不相加。
- 订单总量和销量含取消；销售额不含取消；退款金额来自全部筛选退款事实。退款售后单数不冒充去重退款订单数，平均订单金额不冒充客户维度客单价。
- 未提供可靠口径的客户数、包裹数、毛营收、同期对比不照搬参考页。

## 验证

- 后端 36 项通过（含完整迁移、权限、商品行关联幂等/回滚/租户隔离、SKU 汇总、门店报告完整范围排序分页）。
- 前端 46 项通过，生产构建通过（既有 VueUse PURE 注释警告不影响构建）。
- 本地浏览器验证：快捷日期、销售额降序、展示列、SKU 原编码搜索；1920 宽屏筛选栏单行，390 窄屏页面无横向溢出。恢复默认浏览器尺寸。
- 本地后端已重启加载改动；未启动同步任务。
