# 本地增量：平台商品明细接入仓库 SKU 映射

日期：2026-09-15。仅本地未发布增量，未提交、推送或部署；不修改菜单和路由注册。

## 范围

- 平台商品明细数据的“店铺”表头改为“店铺/仓库”。
- 在现有页面切换店铺商品与仓库 SKU；仓库 SKU 直接来自当前租户可见的极风库存快照，不创建另一套商品档案，不支持在仓库视图导入店铺商品 ID。
- 每个仓库/站点/来源 SKU 取最新快照，支持仓库、来源/内部新旧 SKU、关联状态筛选及服务端分页。
- 映射面板展示历史或精确匹配建议。只读 GET 不写关联；同步沿用已有唯一精确匹配规则。
- 人工确认须具有 integrations.product_mapping.confirm 权限，并检查查看范围与确认范围均覆盖受影响历史。旧关联集合变化时拒绝提交；同一目标重放不重复写入。
- 确认仅更新 internal_sku，不改变库存数量、快照时间或原始负载。按仓库加锁（与同步写入共享锁），保留审计，审计失败回滚。
- 后续同步优先继承仓库历史决定，包含人工确认的编码别名；无历史时继续检查卖家编码冲突和唯一精确匹配。

## 验证

- 后端仓库映射、自动关联及库存分析：60 项隔离测试通过。
- 前端仓库映射与原平台商品页面：16 项测试通过。
- 前端构建通过；菜单注册仍为 101 项、路由 133 项。
- 未进行已登录浏览器人工确认实测，未改动真实业务 SKU 关联。
- 无新增数据库迁移。此登记不表示虚拟机已发布。
