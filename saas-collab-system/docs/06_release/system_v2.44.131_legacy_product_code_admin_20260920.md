# V2.44.131 旧商品编码管理员编辑权限发布登记

- 登记日期：2026-09-20（Asia/Shanghai）
- 状态：`REGISTERED_FOR_CONTROLLED_RELEASE`
- 发布通道：虚拟机功能发布通道
- 父版本：`V2.44.130`
- 父提交：`0fe16449c7879d1f89d77ecb14032f3945d2978d`
- 功能提交：`f38896bf818e28936775e9c59f09308b92e6410c`
- 数据库迁移：无

## 变更范围

1. 商品主数据编辑窗口增加旧 SPU 编码字段；平台超级管理员和租户管理员可以编辑，其他用户只读。
2. 商品明细单 SKU 编辑页的旧 SKU 编码采用同一权限规则；管理员可编辑，其他用户只读。
3. 前端只对管理员提交旧编码字段，后端再次校验权限；非管理员绕过界面修改时返回 403。
4. 旧 SPU、旧 SKU 编码变更写入操作日志，保留变更审计记录。

## 验证记录

- `frontend/tests/product-sku-editor.spec.js`：4 项通过。
- 后端管理员权限及审计测试通过；相关商品明细字段测试通过。
- Vite 生产构建通过。
- 菜单权限快照检查通过：104 项菜单、136 项路由。
- `git diff --check` 通过。

## 部署与验收

1. 通过受保护 PR 合并到最新 `main`，由受控 CI 构建不可变前后端镜像；禁止在虚拟机直接覆盖源码。
2. 部署前确认虚拟机运行版本与 `v2.44.130-deployed` 一致，并创建正式数据库备份。虽然本版本无迁移，仍执行迁移状态检查。
3. 部署后核对四个运行容器 revision、镜像摘要、健康检查、数据库迁移状态和双账本均指向同一 V2.44.131 正式合并提交。
4. 分别以管理员和普通用户验收商品主数据旧 SPU 编码、商品明细旧 SKU 编码的编辑权限，并检查越权请求返回 403、管理员修改产生审计日志。
5. 发布成功后创建不可变标签 `v2.44.131-deployed`，并将实际合并 SHA、镜像摘要、备份、部署时间和验收结果写入发布账本。

## 回退

页面或接口发生回归时，按受控流程恢复 V2.44.130 的不可变应用镜像。该版本没有数据库迁移，不执行数据库反向迁移，也不覆盖部署后新增业务数据。
