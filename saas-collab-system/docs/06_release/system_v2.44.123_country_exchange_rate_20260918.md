# V2.44.123 国家参考汇率

## 父版本

- 虚拟机已部署版本：`v2.44.122-deployed`
- 父提交：`9cd9d1fabd70405bf948690925ab50a0db86dbf5`

## 发布目标

- 为国家档案缓存每日 CNY 参考汇率、数据日期、来源和更新时间。
- 页面按“1 CNY 可兑换的目标币种数量”展示参考汇率。
- 每日通过固定双数据源顺序刷新；主源失败后才使用备用源。

## 变更边界

- 数据库迁移：`masterdata.0014_country_exchange_rate`，仅新增四个可空字段。
- 无新菜单、路由或角色权限；菜单与路由数量保持 `104 / 136`。
- 不包含销售换算、集成自动刷新、同步任务或产品/库存变更。
- 不写入凭据、令牌、生产配置或测试数据。

## 验收要求

- 汇率方向、双源降级和国家档案更新测试通过。
- Django 系统检查、迁移漂移检查、前端测试与生产构建通过。
- 受保护 CI 通过后，使用不可变镜像部署并核对运行 SHA、迁移、健康检查和双版本流水账。

## 回滚

- 应用回滚到 `v2.44.122-deployed` 对应镜像。
- 新增字段保持向后兼容；如需回退数据库，先停止汇率任务，再反向执行 `masterdata.0014`。
