# 本地销售订单表头排序

- 截图中的平台、门店名称、站点、平台订单号、平台原始状态、订单状态、下单时间、币种、订单金额、销售数量共 10 列支持升序、降序、取消排序；操作列不排序。
- 使用既有表格排序控件，后端白名单映射并在分页前排序；保留租户与门店权限过滤，相同值以订单 ID 稳定排序。
- 翻页保留排序，切换排序返回第一页，重置筛选清除排序；取消排序恢复下单时间倒序。
- 后端 12 项、前端 22 项测试通过，构建通过，菜单与路由未改动。
- 确认无运行中的同步记录后重启本地后端加载改动，同时加载此前待生效的自然日采集修正。未触发同步，未部署虚拟机。
