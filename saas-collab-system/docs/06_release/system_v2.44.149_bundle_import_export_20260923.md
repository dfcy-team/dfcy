# V2.44.149 组合商品导入导出入口与图片字段

## 版本登记

- 登记日期：2026-09-23（Asia/Shanghai）
- 状态：`REGISTERED_CANDIDATE_PENDING_V2.44.148`；尚未合并、部署或记入生产账本
- 已部署基线：`V2.44.147`，标签 `v2.44.147-deployed`，提交 `a70ab2a5b95b69df31e87b37a966e0c2b7c1df1c`
- 前序候选：`V2.44.148` 已由库存首期报表 [PR #192](https://github.com/dfcy-team/dfcy/pull/192) 登记；本候选版本号顺延，最终发布须建立在 V2.44.148 的实际合并与部署基线上
- 候选分支：`codex/bundle-import-image`
- 候选 PR：[#191](https://github.com/dfcy-team/dfcy/pull/191)
- 功能提交：`f4b4a44`、`d638aff`；最终候选以 PR HEAD 为准
- 数据库迁移：无

## 变更范围

1. 组合商品 CSV 导入模板增加可选“图片URL”列。导入时校验 HTTP(S) 链接，使用现有图片缓存接口保存组合 SKU 主图；生成的 BigSeller 文件使用缓存后的图片地址。图片处理失败单列行号和原因，不把已创建的组合商品误计为整行失败。
2. 商品明细页顶部“导入与导出”菜单统一放置组合商品导入、旧组合关系迁移和 BigSeller 组合 SKU 导出。移除“新增商品”中的批量新增组合商品项，以及组合商品抽屉内重复的导入导出菜单。
3. 组合导出根据商品明细列表勾选的组合 SKU 逐一读取完整成分，输出子 SKU 编码、数量和成本价分摊比；成分缺失时阻止生成文件。
4. 组合商品导入模板补充可选旧 SPU 编码和旧 SKU 编码；仅平台超级管理员或租户管理员可通过导入写入，旧编码随原子创建保存到对应组合 SPU/SKU。

## 候选验证

- 前端相关定向测试：26 项通过。
- 后端组合商品原子创建与旧编码管理员权限定向测试：2 项通过；数据库迁移漂移检查无变化。
- 前端生产构建及菜单权限快照：通过，`107` 项菜单、`140` 项路由。
- `git diff --check`：通过。
- PR 完整 CI 尚在运行，正式发布前须复核最终结论。

## 发布与账本边界

本文件仅完成 V2.44.149 候选登记，不代表 PR 已合并或虚拟机已部署。PR #191 当前仍以 V2.44.147 为 Git 基线；在 V2.44.148 库存增量合并后，须更新 PR #191 的基线并重新验证，确保两批功能同时保留。正式发布须使用受保护 PR 的最终合并提交构建不可变镜像，核对虚拟机现运行版本与备份，完成部署验收后才创建 `v2.44.149-deployed` 标签并登记虚拟机统一账本和共享账本。不得把当前候选提交当作生产提交。
