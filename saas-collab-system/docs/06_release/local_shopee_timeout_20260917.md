# 本地 Shopee 连接超时调整

- 用户确认调整后，将本地数据库 `saas_collab_live_local_20260909` 中 Shopee 接入配置 5 的连接超时由 3 秒改为 10 秒；读取超时保留 8 秒。
- 通过任务 12、主体 SP10PH 定位配置，并以事务锁及更新后回读验证。此配置共用任务为 8、9、10、12、13、14，后续新请求均使用新连接超时。
- 未修改采集范围、启停状态、代理、授权、重试次数；未触发同步或补采，未修改虚拟机。
- 依据：原连接上限下单页探测在连接/TLS 阶段约 3 秒超时；较宽诊断上限下同类请求成功。不能据此保证整批采集必定完成。
- 本次为用户授权的本地维护脚本操作，不冒用业务用户写入审计 actor；此文档保留变更记录。需要回退时仅将配置 5 的连接超时恢复为 3 秒。
