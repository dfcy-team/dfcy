# 本地增量：同步任务采集范围（2026-09-17）

- 仅本地，未部署、未更改实际任务配置、未触发同步。
- 任务配置与定时抽屉新增订单/退款采集范围：每次回看最近 N 天，或选择起止日期（北京时间，包含结束日；今天截至执行时刻）。日期精度到日，无需输入时分秒或时区；旧带时区配置保持后端兼容，编辑后按日保存。
- 单次范围上限：Shopee 15 天，其余当前订单/退款客户端 30 天；超限、倒置、无时区或未来结束时间拒绝。较长历史须分段设置采集。
- 商品和库存任务保持现有策略，不显示无效的订单/退款范围输入。
- 修正未配置时页面显示 30 天但执行为 1 天的不一致，默认保持实际 1 天，不静默放大业务范围。
- 执行端使用指定起止时间；范围预览统一输出 UTC，滚动窗口在实际执行时重新计算。
- 修改查询策略后清空旧分页游标与检查点内的分页位置，保留历史运行及成功时间；保存不执行、不改变启停状态。
- 固定范围保存后会被后续运行重复使用；补采后需改回滚动范围。Shopee 订单按更新时间，退款按申请创建时间；并非未结退款全量刷新策略。
- 验证：采集范围/调度/任务闭环测试 34 项，平台只读合约及真实只读适配回归 18 项，前端组件测试 8 项，构建通过。未进行真实 API 采集或登录态页面验收。
- 不新增迁移或菜单，原本地未发布增量保留。
