# 本地仓库生产准入管理（2026-09-10）

范围：仅本地 V2.44.83 工作区增量；未发布虚拟机或阿里云。沿用现有后台组件和权限，不新增业务表、不迁移凭据、不自动审批、不调用平台 API。

## 修改

- 生产环境配置增加“极风 WMS · 仓库”：只保存 `platforms.jifeng_wms.contract_approved`，使用现有待审批版本、独立审批人和回退流程。公共字段及密钥仍在连接配置；Email、一次性 Token 仍在仓库档案 API 接入。
- 生产准入增加极风配置行：合同状态、具体阻塞原因、配置级只读网络审批/撤销、公共配置与仓库授权入口；不要求商城回调地址，不提供伪造的合同版本修复。
- 审批接口允许极风配置，保留当前用户、租户/数据范围、确认、版本冲突、幂等及审计；不开放写同步。
- 仓库审批与真实授权共用安全检查：非 DEBUG、模块启用、全局网络及安全审批、加密托管、配置审核、公开字段完整性、实际 API 域名白名单、合同审批。授权及库存读取前仍检查这些条件。
- 极风合同以已生效的系统配置版本为唯一来源，旧 `platform_config.contract_approved` 不再放行。历史库没有新项时默认未批准；不改写既有授权或凭据。首次使用需管理员明确批准新项。
- 公共配置审批不依赖尚未取得的仓库授权或选填外部仓库编码；指定仓库库存读取仍需补齐外部编码。

## 验证

- 隔离 SQLite、禁用外网：54 项通过，覆盖仓库门禁、生产准入 API、生产配置版本、仓库凭据保存。
- 覆盖合同未批时拒绝、审批后生效、旧合同标记不能绕过撤销、跨租户 404、无权限 403、无确认 400、版本冲突 409、重复审批幂等、审计实际用户、关闭安全门后仍可撤销。
- 前端两组测试 11 项通过；菜单权限快照校验和 Vite build 通过。
- 使用现有登录会话只读检查：仓库合同卡片、管理入口、极风准入行、未就绪时禁用审批按钮、仓库档案跳转正常。未执行真实审批、授权、连接校验或同步。未完成多尺寸截图和普通岗位浏览器验收。
- 本地 8002 已重启；未认证准入请求为 401；重启前无待处理 OAuth 会话。

## 当前仍需管理员处理

本地配置 4 的实际 API Host 为 `thtb.jfwms.com`。新门禁查得：极风合同未批准、此域名未加入运行时出站白名单、配置级网络访问未批准（因此只读审批未完成）。

先核对 API Base URL 是否确为服务商提供的接口域名，再在生产环境配置中提交合同/白名单版本并完成独立审批；之后到生产准入审批该配置的只读网络。受控出口代理也必须允许相同的已核定域名；页面白名单不替代机器级出口策略。本次未修改任何实际白名单。

审批只是允许继续授权，不代表 Token 兑换或库存连接已成功。后续真实联调需使用仓库 API 接入，遵守一次性 Token 不重放规则。
