# 系统治理生产就绪复核（2026-09-02）

## 结论

- V2.44.58 候选以虚拟机实际运行的 V2.44.57（`5a71c738be847afd112af0a84e5174714a1dc31a`）为唯一父版本，已补齐系统治理 9 个菜单的真实能力。
- 当前虚拟机运行态仍是整改前能力，不可作为本次整改已上线的证明：日志审计 API 返回 404，配置版本和日志审计页面仍含占位能力。
- 旧账本 `current-version.json` 的所有者已从异常的 `root:root` 修复为 `dfcy01:dfcy01`；发布使用 V2.44.57 已启用的 GitHub canonical 受控工作流，不使用 V2.44.47 遗留候选 manifest。

## 虚拟机复核证据

- 入口 `https://192.168.2.10:8443/` 返回 200，内部健康接口返回 200。
- 组织、用户、角色、安全运维、配置定义和配置变更日志路由存在并进入认证/授权处理。
- `/api/internal/audit/operation-logs/` 返回 404，证明日志审计后端尚未部署。
- 当前前端资产中的配置版本和日志审计仍包含“仅展示/占位”文案。
- CI 控制账本、容器 revision 和 `v2.44.57-deployed` 标签一致指向 `5a71c738be847afd112af0a84e5174714a1dc31a`；旧 unified/legacy 版本指针仍停留在 V2.44.56，需在部署后与 canonical 控制账本同步。

## 9 个菜单整改状态

1. 组织架构：新增、父级选择、编辑、循环校验、跨租户校验、受控删除和审计齐备。
2. 用户目录：保留既有创建、编辑、密码重置、启停和角色维护；列表预取避免 N+1。
3. 角色权限：权限树、数据范围、custom 范围租户校验、启停和受控删除齐备。
4. 安全运维：展示真实账号状态并复用用户管理权限执行启停，写操作有审计。
5. 配置中心：真实创建、敏感值引用约束、全范围权限门禁、双人审批和状态一致性齐备。
6. 配置版本：不可变版本、真实回滚、定义级并发锁和有效版本切换齐备。
7. 平台准入：沿用 V2.44.57 Shopee 生产只读准入能力，未放开生产写入。
8. 发布合同：沿用既有合同、门禁、审批、构建和发布动作能力。
9. 日志审计：真实列表、筛选、分页、详情、租户/数据范围隔离、双层脱敏和安全 CSV 导出齐备。

## 验证结果

- Django system check：通过。
- 迁移漂移检查：`No changes detected`。
- 后端全量：`864 passed, 28 skipped`。
- 前端全量：`46 files, 283 tests passed`。
- 前端 production build：通过。
- `git diff --check`：通过（仅工作区换行提示）。

## 受控上线清单

1. 通过 GitHub canonical 受控工作流执行 `check`，禁止复用 V2.44.47 遗留 manifest 或绕过强制命令入口。
2. 从 V2.44.57/main 生成 V2.44.58 独立候选，记录提交 SHA、镜像 digest、构建清单和回滚目标；不得覆盖 V2.44.57。
3. 发布前备份数据库并验证恢复点；执行新增迁移：
   - `audit.0002_operationlog_query_indexes`
   - `permissions.0036_seed_operation_audit_permissions`
4. 先在候选/预发布环境执行迁移、Django check、后端专项、前端构建和未登录 401/403/404 边界检查。
5. 生产切换后以有权限和无权限账号分别验证 9 个菜单；重点验证跨租户不可见、配置审批者不得为创建者、回滚生成新版本、审计导出上限和脱敏。
6. 若迁移、健康检查、权限目录、菜单路由或审计脱敏任一失败，立即停止流量切换并按候选版本的受控回滚记录恢复。

