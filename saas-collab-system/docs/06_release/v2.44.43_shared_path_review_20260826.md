# v2.44.43 公共路径架构复核说明

日期：2026-08-26
状态：请求架构员决定是否转入 shared review；本文不扩大开发A白名单

## 1. 已从候选净增量移除

| 路径 | 处理 |
|---|---|
| `.gitignore` 中 `.impeccable/` | 与业务增量无关，已撤销；本地目录仅在开发机 `.git/info/exclude` 忽略 |
| `backend/schema/local_formal_merge_20260824_schema.sql` | 完整本地结构快照，已从候选删除 |
| `frontend/src/layouts/MainLayout.vue` | Next.js 外部跳转无调用方，已恢复基线实现 |
| `frontend/vite.config.js` | 本地 `/api` 开发代理已撤销，不进入候选 |

## 2. 申请架构复核的公共路径

| 路径 | 当前调用方与用途 | 权限/兼容性影响 | 测试覆盖 | 回滚方式 |
|---|---|---|---|---|
| `.env.example` | 后端与 worker 的凭据托管示例 | 只记录非敏感路径；不提供密钥 | 凭据维护与只读同步测试 | 恢复基线示例，不影响运行数据 |
| `backend/config/settings/base.py` | integrations、reports 读取只读同步、平台路径、托管和导出设置 | 默认生产仍拒绝未批准实时访问；新增设置均有安全默认值 | Django check、凭据维护、只读同步、报表测试 | 恢复基线代码并关闭对应环境开关 |
| `docker-compose.yml` | backend 与 worker 挂载本地凭据托管卷 | 仅开发编排；不传递明文凭据 | Compose 配置静态复核 | 恢复基线编排，保留卷到审计完成 |
| `backend/apps/permissions/api_permissions.py` | integrations 生产只读运行端点 | 新增独立 `integrations.run_live_readonly` 权限，不复用普通运行权限 | integrations 权限与正式只读测试 | 移除端点引用后恢复基线类定义 |
| `backend/apps/permissions/services.py` | 授权服务允许识别新的只读运行权限码 | 不自动授予角色；保持 tenant/role 边界 | 权限服务与 integrations 测试 | 先撤销角色授权，再恢复基线权限集合 |
| `frontend/src/api/request.js` | 全部前端 API；新增真实文件下载并禁止生产 Mock fallback | 401 刷新和 tenant 权限不变；生产失败不再伪装为 Mock 成功 | 前端全量测试、销售导出、workflow Mock 隔离 | 调用方先恢复占位下载，再恢复基线请求层 |
| `frontend/src/api/uiP6Adapters.js` | 经营分析库存风险筛选映射 | 仅把受控枚举 `risk_level` 映射为后端 `risk` | `ui-p6-api-analysis.spec.js` | 恢复基线映射，页面不再传风险筛选 |
| `frontend/src/components/AdminResourcePage.vue` | 平台、国家、店铺、仓库等基础档案页面 | 操作仍由既有 manage permission 和后端 data_scope 控制 | 基础档案交接、UI-P2 前后端测试 | 先恢复调用页 props，再恢复基线组件 |
| `frontend/src/components/Phase2DataPage.vue` | API 接入/同步等多个阶段2只读页面 | 只扩展展示维度，不新增写权限 | integrations、阶段2页面测试 | 恢复调用页参数后恢复基线组件 |
| `frontend/src/components/Phase3AnalyticsPage.vue` | 经营分析三页共享筛选、指标和表格展示 | 仍只调用现有只读 API；筛选受后端 data_scope 约束 | `ui-p6-api-analysis.spec.js` 与分析后端测试 | 恢复三个分析调用页后恢复基线组件 |
| `frontend/src/views/reports/ReportExportCenter.vue` | 通用报表中心边界文案 | 明确通用报表仍为占位下载，真实销售导出留在销售管理 | workflow/report 前后端测试 | 恢复基线文案和操作说明 |

## 3. 架构决策请求

1. 上述 11 个路径不得按开发A直接运行路径放行。
2. 架构员应逐项决定：加入与候选 commit 绑定的 shared review，或改由架构发布通道交付。
3. 若任一路径被拒绝，必须先同步撤销依赖它的调用方，再生成新候选材料。
4. `request.js`、全局权限服务和公共组件应重点复核跨模块回归；不得只以单页冒烟代替全量测试。
