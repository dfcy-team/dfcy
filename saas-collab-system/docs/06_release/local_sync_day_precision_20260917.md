# 本地采集自然日精度修正

- 修正运行详情将秒级时间戳当成毫秒导致显示 1970 年的问题；采集日期统一按北京时间展示到日，运行开始/结束时间仍保留 UTC 秒级记录。
- 订单和退款的最近 N 天包含今天，从首日北京时间 00:00 至执行时刻；指定日期继续包含结束日，今天截至执行时刻。
- 不修改历史运行范围，不自动执行任务，不部署虚拟机。
- 验证：后端 42 项、前端 14 项测试通过，前端构建通过。
- 检查时仍有运行中的任务（任务 12，运行 14）；为避免中断采集，未重启本地后端。自然日执行逻辑需在运行结束后重启后端加载。
