# 本地库存明细排序（2026-09-11）

库存分析表头 11 列支持升序、降序、取消排序；使用既有 Element Plus 排序交互，不改变页面设计。仅本地更新。

- 后端 `ordering` 白名单映射界面字段，先选最新快照并筛选，再排序分页；不对当前页单独排序。
- 数量按数字排序、时间按快照时间排序；内部 SKU 空值在两个方向均置后。
- 风险升序：缺货、锁定偏高、低库存、正常；SKU 关联升序：未关联、已关联。
- 同值使用记录 ID 保持稳定顺序。非法字段返回校验错误，不开放任意 ORM 字段。
- 切换排序回第一页，翻页保留排序；取消或重置恢复默认顺序。快速切换时忽略旧请求结果。
- 未更改数量、SKU 关联、授权、任务开关或同步行为。

验证：后端完整迁移回归 72 项通过；随后补充分类/空值排序测试，库存专项 37 项通过。前端 51 项通过。首次跳过迁移运行的 BI 测试因缺少迁移生成的权限种子失败，完整迁移重跑已通过。

真实本地页面核验：Enter 键可切换排序；库存降序首项 4990，第一页末项 498、第二页首项 491，分页顺序连贯；关联升序准确置顶 5 条未关联记录。切换排序回第一页，重置清除排序。全部数据仍为 965 条，已关联 960 条。
