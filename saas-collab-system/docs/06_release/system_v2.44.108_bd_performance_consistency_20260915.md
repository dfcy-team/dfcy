# V2.44.108 BD 绩效一致性修复

日期：2026-09-15。状态：已完成代码与生产数据修复，待受控 CI 镜像部署后登记部署结果。

## 版本基线与范围

- 主分支基线：`85d5069`（V2.44.107 同步任务与运行记录增量）。
- 本版本只修改 BD 绩效读取、前端请求失败闭环及配套测试，不改变菜单、路由或权限目录。
- 前端 BD 绩效请求超时从统一 10 秒调整为 60 秒，并禁止网络失败时静默回落为空 Mock。
- 历史送样投入、状态及发货时间读取冻结的 `BdSampleAttributionSnapshot`，不再随履约记录后续编辑漂移。
- 订单绩效 owner 以关联送样归因快照为权威事实，不依赖可能滞后的订单归因冗余 owner 字段。

## 生产数据修复

- 修复前已执行受控生产备份，结果 `PRODUCTION_BACKUP=PASS`。
- 租户 1 写入 2026-08-01 起生效、来源为 `owner_approved_20260915` 的历史汇率：
  - `PHP -> CNY = 0.1250000000`
  - `MYR -> CNY = 1.6500000000`
  - `THB -> CNY = 0.2050000000`
  - `CNY -> CNY = 1.0000000000`
- 汇率按租户、币种对、生效日期和来源幂等登记，共 4 条；复核无缺失汇率。
- 同步租户 1 的 strict 订单归因冗余 owner 与关联送样归因 owner：更新 9,566 条，残留失配 0。
- 更新前映射哈希：`a214d07eaecdaa49434846b2b7cffc4b86e6228f4a6e5297963b748e3d372790`。

## 2026-08 报表验收口径

- 查询范围：2026-08-01 至 2026-08-31，方式一 strict，CNY。
- 有效订单 309，GMV `34390.1169`，与开发 B 验收截图一致。
- 订单按关联送样快照恢复到吴嘉炜 76、蔡婧妮 147、曹锦发 32、李烨君 34、沈锦培 4 等原归属；不是按截图硬编码。
- 当前有效业务数据包含 523 条 legacy 送样和 11 条原生送样，合计 534 条、13 名相关人员。11 条原生记录没有可证明的完整事实键重复，因此保留，不删除真实业务记录。
- 历史投入改用冻结成本后，部署版本不再受履约成本后续刷新影响。

## 验证结果

- `python manage.py check`：通过，0 issues。
- `python manage.py makemigrations --check --dry-run`：通过，无迁移漂移。
- 后端 BD 绩效/归因/冻结成本专项：9 项通过；新增 owner 权威来源测试在更正送样 owner 后验证报表不读取滞后订单 owner。
- 前端 BD 绩效专项：17 项通过。
- `VITE_USE_MOCK=false npm run build`：通过，2163 modules；菜单权限快照保持 101 项菜单、133 项路由。

## 部署边界

- 必须使用受控 CI 构建的不可变镜像部署，禁止虚拟机本地构建源码。
- 部署后以 root、租户 1、2026-08-01 至 2026-08-31 实测页面；接口应返回真实数据，不得出现 `mock`/`degraded`。
- 部署后再次核对 4 条汇率、strict owner 残留失配为 0、有效订单 309、GMV `34390.1169`，再将状态更新为已部署。
