# V2.44.151 库存工作台与库存分析交互候选登记

## 版本登记

- 登记日期：2026-09-23（Asia/Shanghai）
- 状态：`REGISTERED_LOCAL_CANDIDATE`；仅本地候选，未提交 PR、未合并、未部署虚拟机，未记入生产账本
- 当前主干父基线：`V2.44.150` 商品成本导入，主干提交 `98b2648`；该版本已合入主干，尚未在本次核查中确认虚拟机部署或 `v2.44.150-deployed` 标签
- 最近可核实的已部署标签：`v2.44.149-deployed`，提交 `29cdd5a1ffdc6ce9098e430e916f0daa0fdf4550`
- 候选分支：`codex/v244151-inventory-workbench`
- 候选功能提交：`5476b68`；正式版本提交应以受保护 PR 的最终合并提交为准
- 数据库迁移：无新增迁移

## 本版范围

1. 库存管理新增按业务对象呈现的只读“库存工作台”菜单，提供库存概况、仓库分布、风险与数据质量视图，并可下钻库存分析。
2. 工作台接口沿用现有极风 WMS 库存快照与内部 SKU 关联数据，支持相关仓库和 SKU 过滤；无真实快照时显示空态，不伪造库存或补货建议。
3. 库存分析“包含虚拟商品”默认不勾选；保留用户主动勾选查询能力，并完善相关筛选重置与下钻交互。
4. 菜单、路由和权限快照同步增加库存工作台；基础档案四项导出属于既有版本，不纳入本次改动。

## 本地验证

- 基于 V2.44.150 主干重放候选后，前端完整测试 `122` 个文件、`742` 项通过；生产构建通过。
- 菜单权限快照检查通过：`108` 项菜单、`141` 项路由一致。
- 后端库存工作台定向测试：`5` 项通过。
- 本地浏览器已检查菜单可见、角色权限隔离、仓库图表下钻及桌面/移动端页面；这不替代虚拟机真实数据与角色验收。

## 发布闸门与边界

本登记不表示生产可用。后续须完成受保护 PR/CI、虚拟机当前镜像与双账本核对、部署前备份和受控发布，并在真实业务账号下验收菜单权限、库存指标与下钻。此前用户报告的库存分析接口 `400 VALIDATION_ERROR` 尚无生产修复验收结论，发布前须单独复核其请求参数和服务端响应，不能因本地测试通过而视为已解决。正式部署成功后才可更新状态、记录不可变镜像摘要与发布回执。
