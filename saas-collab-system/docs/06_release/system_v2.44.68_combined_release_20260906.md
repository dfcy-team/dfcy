# V2.44.68 开发 A、开发 B 与 API 补全合版登记

- 日期：2026-09-06；发布目标：应用虚拟机 192.168.174.131，业务核验以租户 1 为准。
- 用户授权：将开发 A、开发 B 和 API 内容补全的增量进行版本登记并部署虚拟机。
- 唯一父基线：V2.44.67，Git `87886c3716b63f40efb8e5cc4a338cff47894862`，标签 `v2.44.67-deployed`。
- 隔离合版分支：`release/v24468-deva-devb-api`。
- 本文件是候选和核定范围记录；实际部署提交、镜像摘要、备份、时间和复核状态由虚拟机双账本及发布记录登记，不以候选构建代替上线结果。

## 来源与增量

| 来源 | 核定输入 | 本版内容 |
| --- | --- | --- |
| 开发 A | PR #89，`3689d5233de4ff2edf190218f8d8560524ba57c9` | 接入工作区接受 configured 凭据；禁用配置不计入可核对主体；过期及撤销凭据回归覆盖 |
| 开发 B | PR #86 的增量提交 `89dac0a`、`31d05845070d4e2a99ff45177493a1aee833f05d` | 每日逾期送样核对、送样达人进度、重复送样提示、黑名单拦截、任务完成状态核对命令 |
| API 补全 | `v24467-mapping-consolidation` 的 57 个已核对候选文件，导入隔离提交 `3266f1c` | 店铺/仓库外部身份唯一性、平台商品只读同步入档、新旧 SKU 精确一致性及冲突处理、页面同步上下文与来源显示 |

API 候选原父提交 `9542abc` 与正式 V2.44.67 的业务源码一致，仅旧发布说明有差异。合版从正式提交重建，只取候选文件增量及开发 B 两个业务提交，未合入旧分支对 V2.44.67 的反向差异。

## 菜单、权限与部署边界

- 菜单名称、顺序、路由、导航 CSS、角色权限目录及生产控制代码与 V2.44.67 一致。
- 本版使用已有商品、接入、仓库及达人页面，没有新增菜单节点或权限码。
- 商品同步保持租户、店铺、资源类型及操作权限交集；已有订单范围权限不自动获得商品执行权限。
- 新商品接口合同审批默认关闭，Shopee/TikTok 真实商品只读执行仍需独立完成合同、凭据、网络及能力审批。
- 真实 WMS 连通性不作为本次代码上线结论；未执行真实平台同步或导入业务数据。
- 每日送样核对保留 86400 秒间隔；沿用 VM 已核定的 Beat 持久卷 `saas-collab-celery-beat-state`，避免重建后丢失调度时间。
- 新任务完成核对命令默认只读；本次发布不自动执行历史任务回填命令。

## 数据库迁移及回退

1. `masterdata.0013_store_external_identity_constraint`：外部店铺身份预检、回填及唯一约束。
2. `integrations.0023_warehouse_external_identity`：外部仓库身份、历史绑定回填及唯一约束。
3. `integrations.0024_platform_product_readonly_sync`：商品同步资源及可信精确关联来源。
4. `configcenter.0003_product_readonly_endpoint_defaults`：补充商品端点与默认关闭的独立审批字段。

迁移前已在真实旧库只读运行租户 1 外部身份报告：店铺重复组 0、仓库重复组 0、仓库缺失身份 0。真实数据库版本 MySQL 8.4.10。发布前须以同版本隔离 MySQL 对备份完成迁移演练；不得为了通过约束删除或合并现有业务档案。

应用回退使用 V2.44.67 不可变镜像；新增字段及配置按前向兼容保留，关闭新商品资源。若必须完整恢复数据库，使用发布前受控备份并由架构员核对备份后新增业务数据，禁止自动反向清除业务记录。

## 验证记录

- 前端全量：67 文件、409 项通过；`VITE_USE_MOCK=false npm run build` 通过。
- 生产依赖审计：`npm audit --omit=dev --audit-level=high`，0 vulnerabilities。全依赖审计另有开发期传递依赖 brace-expansion 的既有高风险通告，不进入生产运行包；本版未扩展依赖升级范围。
- 受保护目录差异：菜单、路由、布局、权限目录、部署及工作流与父基线差异为空。
- Playwright（Browser 插件不可用，使用现有 Edge/Playwright）：本机隔离 Mock `http://127.0.0.1:4188`，1600×1050 与 390×844；商品行进入限定店铺的商品同步任务、外部仓库编码换绑、仅查看角色隐藏同步按钮均通过，无页面异常或失败响应。
- 页面截图与复核辅助材料：工作区 `发布控制/V2.44.68发布_20260906/ui-evidence/`。这是隔离页面证据，不代表真实平台已连通。
- 后端全量：1049 passed、28 skipped；新增范围定向 65 passed；Django check、迁移漂移检查及从零迁移全部通过。
- 达人命令修订：明确视频档案刷新默认 dry-run、只有 `--apply` 才持久化；定向后端 4 passed、前端 18 passed。未改变既有生产命令执行语义。
- 租户 1 发布前权限快照：权限目录 243 项、角色 21 个、角色授权关系 372 条、用户角色关系 17 条、数据范围 21 条；保存各集合摘要供发布后比对。
- 同版本 MySQL 迁移演练及发布后检查结果，在核定完成后补充。
