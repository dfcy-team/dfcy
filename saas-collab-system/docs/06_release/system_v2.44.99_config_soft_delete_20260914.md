# V2.44.99 接入配置软删除重建修复（候选，未部署）

日期：2026-09-14。基线：V2.44.98，dba1ebbb7a9f2380150cc5e61af73659e872e554。

## 修复内容

原唯一键包含租户、平台、名称、环境，但未区分软删除记录。创建入口看不到已删除配置，数据库却仍保留其名称占用，造成同名重建 HTTP 500。

新增数据库生成列 active_uniqueness_marker：未删除时为 1，已删除时为 NULL；唯一键增加该列。先建立新约束再移除旧约束，保留有效配置唯一性。普通创建、工作台创建和修改入口捕获特定唯一键竞争，返回 409；其他数据库错误不吞掉。

不删除、改名或恢复历史配置；不转移旧授权、任务、仓库关联或审计。重建生成新 ID，凭据和仓库绑定仍需管理员按原流程维护。本次不增加外部 API 调用或生产写入权限。

## 角色权限登记

沿用 integrations.config.view/create/update/disable 及原数据范围校验，无新增权限、角色授权或菜单。系统管理员仍需具备对应权限；不因本修复放宽租户边界。删除入口原权限要求不变。

## 验证及环境

隔离 MySQL 8.4.10：从 integrations.0026 迁移到 0027，历史配置 3 行迁移前后字段哈希一致，审计哈希一致；连续两轮同名重建成功，有效重复被数据库拒绝。测试数据为合成数据，没有在生产业务库试写。

SQLite 回归初版 3 项通过（106.96 秒）；覆盖两类创建入口、删除重建、修改冲突、跨租户及历史关联保留。最终将工作台用例调整为 jifeng_wms / inventory / production / MY，并加入唯一键错误分类测试：MySQL 接口回归 4 passed in 113.97s（dev 配置，--nomigrations 建测试表；实际迁移由独立脚本另行验证）。Django check、makemigrations --check --dry-run 和 git diff --check 均通过。

本次仅验证相关迁移依赖链。全量空库 MySQL 初始化遇到已有 development.0002 事务内 DDL 问题，不属于本修复；不宣称全仓库空库初始化通过。

两台线上环境只读核对均为上述基线：阿里云 MySQL 8.4.11，配置 9 行（有效 3、已删除 6）；原虚拟机 MySQL 8.4.10，配置 3 行（有效 1、已删除 2），有效重复分组均为 0。计数为发布前快照，实施前须刷新。

## 发布与回退约束

待用户审阅确认后，分别按现有发布控制流程执行备份、迁移、应用更新和健康验证。先原虚拟机后阿里云，每台单独验收。保留阿里云当天已批准的 Compose 外部媒体卷和权限修复，不覆盖运行配置、密钥或无关服务镜像。不捎带主分支其他新功能。

MySQL DDL 不能依赖事务整体回滚，需在维护窗口停止配置写入并先备份。迁移后允许同名历史，不能直接恢复旧唯一键；应用回退优先保留新数据库结构。确需回退数据库时，先核对同名历史冲突并另行审批，不清除业务历史。

线上验收：迁移记录、唯一键结构、配置及关联数据保留、服务健康、版本一致性；不得为了验收自动重建真实配置或继承历史凭据。
