# v2.44.43 五模块增量数据发布候选记录

日期：2026-08-25
目标环境：`192.168.2.10` 应用层 VM
发布通道：开发 A 受控发布入口
父版本：`v2.44.42-deployed`
父版本 Git：`3b6fb1042b0da1e89bad0b83540caa6d5c444c32`
候选版本：`v2.44.43`
候选 Git：本文件所在提交（以远端 candidate manifest 登记值为准）

## 发布范围

本候选只登记以下菜单及其 tenant 1 数据联动：

1. 店铺档案：店铺身份、平台、国家、币种、时区、授权映射和同步状态。
2. 仓库档案：仓库、国家、库存授权、同步运行和库存事实关联。
3. 销售管理：销售总览、订单、退款退货、门店销售、SKU 销售、导出和数据质量。
4. 经营决策：Vue Router 菜单直达六个 Vue 原生页面，复用现有登录、RBAC、数据范围和 CSS 结构；不再加载 Next.js 独立工作台或独立样式。
5. 经营分析：Commerce/WMS 真实字段、动态筛选、数据新鲜度、销售与库存维度联动。

API 数据接入、凭据托管、同步任务、同步运行和业务事实的变化仅作为上述五个模块的数据链路支撑，不调整其他业务菜单。

## 数据与结构增量

- 统一使用 tenant 1 的本地平台、国家、店铺和仓库档案作为授权映射主数据。
- 销售事实复用订单、订单明细、退款退货和退款明细；库存事实复用库存快照，不创建平行业务事实表。
- 新增正式只读 Commerce/WMS 合同、同步工作区、事实幂等写入和增量导入命令。
- 数据新鲜度、来源、质量状态和刷新时间由同步运行及事实记录联动计算。
- 包含基础档案 `masterdata.0003` 至 `masterdata.0008`、`reports.0007_real_export_files` 与 `permissions.0032_seed_live_readonly_sync_permission` 共 8 项 Django migration。

## 验证记录

- Vue 专项：20/20 PASS。
- Vue production build：PASS。
- 经营决策六个 Vue Router 页面专项：PASS。
- 后端专项：41/41 PASS。
- Django system check：PASS。
- migration drift：PASS，No changes detected。
- `git diff --check`：PASS。
- 高置信密钥模式与环境变量赋值检查：PASS；未提交真实密码、Token、密钥或证书。

## 受控发布门禁

- 本候选修改了 masterdata、commerce、菜单、路由和权限目录等共享路径，必须取得架构员共享补丁批准。
- 本候选包含 Django migration；开发 A 的 `--publish` 会按规则阻断，需架构员先审核并执行生产 DDL。
- 发布前由远端共享台账再次核对 `parent_version=v2.44.42` 与 `release_version=v2.44.43`；若台账已推进，必须重新编号和更新材料。
- 只有远端 hook 部署成功并原子登记台账后，才可创建对应 `v2.44.43-deployed` 状态记录；候选提交不冒充已部署版本。

## 回滚

1. 保留当前 `v2.44.42-deployed` 镜像和 Git 节点作为回滚基线。
2. 发布失败不推进共享版本台账，不删除已存在事实数据。
3. 关闭正式只读同步开关，停止新增同步任务。
4. 代码和镜像回到父版本；数据库迁移只按架构员批准的回退方案执行。
5. 回滚后核对 tenant 1 的档案、授权映射、同步运行和业务事实链路。

## 后续版本记录规则

后续每次修改继续执行：更新本目录版本记录、生成单一可追溯 Git 提交、推送候选分支、运行远端 `--check`、通过受控 `--publish` 后登记实际 deployed 版本。不得复用旧候选提交、跳过共享台账或在部署成功前标记已发布。
