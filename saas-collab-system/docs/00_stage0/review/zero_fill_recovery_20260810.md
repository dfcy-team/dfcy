# 2026-08-10 本地工作区零填充异常恢复报告

## 结论

本次异常不是普通的单文件误删，而是 Git 元数据、部分源码、文档、依赖目录和本地数据文件被批量覆盖为全零内容。恢复仅在架构员本机及独立恢复分支进行，未连接、修改或部署供应链正式线上系统。

恢复分支：`recovery/zero-overwrite-20260810-094642`。

## 恢复证据与原则

- 远端公开基线：`origin/main`，仅用于恢复可确认的已跟踪基线文件。
- 本机 Git 未引用对象和历史 blob：用于恢复仍完整的本地开发文件。
- Codex 2026-07/2026-08 原始 JSONL 会话：按成功 `apply_patch` 记录顺序重放本地未提交功能。
- 对缺乏可靠来源的证书、密钥、数据库数据、二维码及二进制文档不进行猜测或伪造。
- 原损坏 Git 配置、索引、对象包及工作树索引已保存在 `.git/recovery-20260810-094114`；损坏配置另存为 `.git/config.corrupt-20260810-093838.bin`。

## 已恢复范围

- Git 本地仓库配置、远端基线和独立恢复分支。
- 供应链 F1、运输路线、F2 装箱、多批次、集货、附件、发运、API2、UAT 数据与短期凭据认证。
- Purchasing、Packing、Consolidation、Shipping、Files、Accounts、Products、Development、Listings、Releases 相关模型、服务、API、迁移和测试。
- Web 供应链工作台、产品/刊登/达人/发布合同页面及路由。
- 微信小程序 10 个页面路由的 JS/JSON/WXML/WXSS、核心请求/会话/认证模块、校验脚本及测试。
- 供应链审核基线、合同、报告、API 文档及中文标签字体四文件。
- 缺失的迁移包标记、装箱 `0006`、权限 `0020-0026`、账号并行迁移合并 `0005` 等迁移图缺口。

## 最终验证基线

- Django system check：通过。
- `makemigrations --check --dry-run`：`No changes detected`。
- 全新隔离 SQLite 从零迁移：所有应用迁移通过，包含 Packing 0001-0006、Consolidation 0001-0006、Files 0001-0003、Shipping 0001-0002。
- 供应链与 UAT 聚合回归：`113 passed`。
- 产品/刊登专项：`32 passed`。
- F1/运输路线补充回归：`30 passed, 3 skipped`；跳过项为未启用 MySQL 环境的并发用例。
- Web 供应链定向：`3 passed`；Vite production build：通过。
- 微信小程序：10 个路由四件套完整，32 个 JS 校验通过，28 项测试通过。
- Git diff whitespace gate：通过。

## 无法可信恢复的内容

- `miniapp/.certs/localhost.pem` 与 `localhost-key.pem`：本地证书/私钥已全零，无可信副本，应重新生成，不得复用或伪造。
- 原 `.env`：内容已全零，原机密不可恢复；后续应由管理员从安全凭据来源重新配置。
- 原 `backend/db.sqlite3` 及若干本地开发 SQLite：内容已全零，历史本地数据不可恢复；代码验证使用新建隔离数据库。
- 18 份非供应链 Word/Markdown/二维码/RPA/发布运维材料：无可靠原件，保留损坏证据，需从原作者、归档盘或正式文档库重新取得。
- 三个产品前端页面仅能恢复到有可靠原始 Add 证据的基础版本；构建通过，但后续历史增量因原补丁上下文损坏未冒险拼接，应在后续独立功能复核中核对 UI 完整性。

## 后续门禁

1. 不得将本恢复分支直接部署到正式系统。
2. 先由架构员复核本报告、恢复提交和不可恢复清单。
3. 重新签发本地证书、配置本地 `.env`，并审核小程序 `config/runtime.js` 中历史 Cloudflare 临时域名。
4. 在独立 MySQL 8.4 临时环境执行完整迁移与并发门禁后，才可进入下一业务阶段。
5. 恢复提交完成后生成仓库外 Git bundle，并将恢复分支非强制推送到远端备份。
