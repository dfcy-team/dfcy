# SCM-REQ-V3-R1 剩余P1快速整改报告

- 日期：2026-08-12
- 整改对象：`R3-P1-002A`、`R3-P1-003A`
- 状态：`READY_FOR_SECOND_QUICK_RECHECK`
- 范围：仅需求追踪与冲突登记；不授权业务代码或数据库变更

## 1. R3-P1-002A 网页端原子追踪

已将原12组网页范围ID展开为独立记录：

- `WEB-FR-001`起，覆盖后端4.1-4.16各页面、字段、动作、审核、导出和权限需求；
- `WEB-DATA-*`覆盖5.1-5.3实体、表和视图；
- `WEB-RULE-*`覆盖6.1-6.6；
- `WEB-NFR-*`覆盖7.1-7.4；
- `WEB-AC-*`覆盖8.1-8.2验收；
- `WEB-OUT-*`、`WEB-MAP-*`覆盖9及10.1-10.3；
- `WEB-RATING-001..029`覆盖11.1-11.6场景、页面、公式、异常和8条验收；
- `WEB-APPENDIX-*`覆盖12.1-12.2；
- `WEB-NAV-001`保留用户追加菜单顺序要求。

每行均具有源章节、需求摘要、状态、目标波次以及证据或缺口。自动检查结果：网页范围ID数量为0，网页独立ID匹配245条。

## 2. R3-P1-003A 冲突登记

已新增 `scm_requirement_v3_conflict_register.md`，登记：

1. `SCM-CONFLICT-001` 散货直发/区域集货；
2. `SCM-CONFLICT-002` 路线决定时点；
3. `SCM-CONFLICT-003` 单活动批次开关/多批次；
4. `SCM-CONFLICT-004` 发货5态/Shipment 8态；
5. `SCM-CONFLICT-005` 货柜9态/Container缺失；
6. `SCM-CONFLICT-006` 旧技术描述/当前协同架构。

每项均记录来源、冲突、裁决状态、唯一裁决、影响、迁移要求和下一门禁。合同已解决的业务冲突标记为 `RESOLVED_BY_CONTRACT`，技术适配标记为 `SUPERSEDED_TECHNICALLY`。

## 3. 保持不变的门禁

- 两份桌面需求仍为最新完整业务权威，摘要变化需重新审核。
- 小程序 `MINI-AC-001..081` 仍逐条连续追踪。
- 菜单顺序仍为产品开发、供应链协同、多平台刊登。
- Permission/DataScope、数量守恒、并发幂等、附件安全及正式线上隔离不得弱化。

## 4. 下一步

执行 `SCM-REQ-V3-R1 剩余P1第二次快速复核`。复核通过后，才可把V3改为 `BASELINE_FROZEN_FOR_MAP_1`；本报告本身不授予模型、迁移、API或客户端开发权限。
