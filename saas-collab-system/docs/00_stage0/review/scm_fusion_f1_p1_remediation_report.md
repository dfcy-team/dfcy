# SC-F1 第一次本地代码审核 P1 整改记录

日期：2026-07-25
环境：架构员主机、本地 Docker MySQL 8
边界：未连接供应链正式系统、生产 Supabase、真实微信平台或外部通知通道。

## 整改结果

1. 采购单明细在接单后禁止新增、修改和删除；批量写入、批量更新和 QuerySet 删除路径均被模型层拒绝。
2. 生产进度和采购单事件改为追加写记录；实例更新、批量更新和删除路径均被拒绝。
3. 创建接口捕获 MySQL 唯一键竞争，按租户、幂等键和请求哈希重新读取原订单；同请求并发返回 `201 + 200`，只产生一张采购单和一条创建日志。
4. 业务动作保存请求哈希和原响应快照；重试返回原动作结果，不受订单后续状态变化影响；同键不同载荷返回冲突。
5. `0016_seed_supply_chain_f1_permissions` 已解除对发布模块迁移的直接依赖，并增加当前组合工作树使用的迁移合并节点。
6. Vue 管理端增加服务端分页控件，小程序增加分页参数、触底加载和“加载更多”入口。

## MySQL 专项验证

本机 MySQL 只绑定 `127.0.0.1:3306`。专项测试使用隔离数据库 `test_scm_f1_local_dev`。

| 验证项 | 结果 |
|---|---|
| 并发创建相同幂等键 | 通过，只创建一张订单 |
| 并发执行相同动作 | 通过，只产生一个事件，另一个请求重放 |
| 接单后明细 ORM 绕过 | 通过，新增、批量更新和删除均被拒绝 |
| 事件/进度 ORM 绕过 | 通过，更新和删除均被拒绝 |
| SC-F1 MySQL API 与专项测试 | 11 passed |

## 回归验证

| 验证项 | 结果 |
|---|---|
| Django system check | 通过 |
| Django migration drift | No changes detected |
| 后端完整回归 | 424 passed, 3 MySQL-only skipped |
| Vue 完整 Mock 回归 | 164 passed |
| Vue 生产构建 | 通过 |
| 小程序完整测试 | 26 passed |
| 小程序项目校验 | 通过，8 pages / 28 JavaScript files |
| `git diff --check` | 通过，仅有仓库既有 CRLF 提示 |

## 尚未包含

本记录只关闭第一次审核提出的 P1。Vue Mock 严格状态机和网页端多明细录入属于 P2，留待后续整改。

本次结果仅用于架构员主机本地开发和第二次代码审核，不构成测试、预览或生产环境发布授权。
