# SC-SUPPLY-FLOW 提交后基线确认

- 日期：2026-08-08
- 审核基线：`40661ee8a23aad91d7ec8fde1ee7e15cbcace97c`
- 功能提交：`e1333fa045221919ad3debfadcae4a097a093cea`
- 边界修复提交：`40661ee8a23aad91d7ec8fde1ee7e15cbcace97c`
- 审核环境：架构员本机隔离工作树，未连接生产系统
- 结论：`APPROVED_AS_LOCAL_SUPPLY_FLOW_BASELINE`

## 1. 提交范围确认

本基线包含供应链多批次与部分装箱、散货集货、受控交接附件、拼柜发运、API2、权限与 DataScope、内部 Web 和供应商微信小程序适配，以及对应迁移、测试和审核材料。

提交采用逐文件与共享文件逐补丁块白名单，未纳入当前工作区的账号、产品开发、刊登、达人、RPA、凭证托管及其他并行阶段修改。`products.0014` 和其迁移门禁测试依赖未提交的产品迁移链，因此未纳入本供应链基线；提交前报告中有关该迁移的验证仅代表当时本机工作区证据，不代表本提交内容。

## 2. 迁移独立性整改

首次在提交 `e1333fa` 的干净隔离工作树检查时，发现两项并行迁移依赖：

1. `permissions.0025` 依赖未纳入提交的 `permissions.0024`；
2. `consolidation.0006` 依赖未纳入提交的 `masterdata.0002`。

边界修复提交 `40661ee` 将其分别调整为已提交且满足模型需求的 `permissions.0019` 与 `masterdata.0001`。修复未改变权限种子内容、领域模型或业务状态机。

## 3. 提交后隔离验证

从 `40661ee` 创建独立 detached worktree，不读取主工作树未提交源码，并使用 SQLite 正常迁移路径执行：

- `manage.py check`：通过，0 issues；
- `manage.py makemigrations --check --dry-run --noinput`：通过，`No changes detected`；
- 供应链核心矩阵：`74 passed in 37.47s`。

核心矩阵覆盖 packing services/API、F2 multi、本地集货、受控附件、shipment 和 supply-flow API2。隔离工作树和临时数据库在验证后已删除。

提交前已归档的 MySQL 8.4、Web 构建与小程序测试证据继续作为本阶段辅助门禁证据；本次提交后确认以干净 Git 基线的迁移可解析性和 SQLite 核心矩阵为权威增量证据。

## 4. 线上隔离与未准入事项

本次仅形成架构员本机 Git 基线，未推送、未部署、未执行生产迁移、未连接生产数据库或正式供应链系统。

以下事项仍未准入：生产对象存储与病毒扫描、下载票据、真实货代/报关/船司/承运商接口、自动报关或自动清货、Android/iPhone 真机最终验收、微信正式发布和生产切换。

## 5. 下一阶段准入建议

当前基线可进入“供应链本地用户验收准备与测试数据方案冻结”。进入生产部署、灰度或外部系统联调前，必须另行完成环境隔离审核、迁移演练、回滚方案、生产凭证审批和移动端真机验收。
