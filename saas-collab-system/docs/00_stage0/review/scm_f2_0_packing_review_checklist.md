# SC-F2-0 装箱领域契约审核检查表

## 1. 审核信息

| 项目 | 值 |
|---|---|
| 工作包 | `SC-F2-0` |
| 目标分支 | `codex/scm-f2-packing-local` |
| 父提交 | `774fb3568220349e70a8345f93b73f442f8ba22e` |
| 主契约 | `docs/00_stage0/review/scm_f2_0_packing_domain_contract.md` |
| 审核范围 | 契约、边界和本地开发准入 |
| 功能编码 | 未授权 |
| 生产操作 | 未授权 |

## 2. 基线与隔离

- [x] F2 使用独立本地分支。
- [x] 父提交固定为 SC-F1 最终审核归档提交。
- [x] 源网页端附件 SHA-256 已记录。
- [x] 源小程序端附件 SHA-256 已记录。
- [x] 目标代码树哈希已记录。
- [x] 原有未提交工作区内容不属于 F2 提交范围。
- [x] 未连接或修改供应链正式线上系统。
- [x] 未导入真实业务数据。
- [x] 未执行 Supabase migration、RLS、RPC 或 Edge Function。
- [x] 未发布微信小程序。

## 3. 领域边界

- [x] F2 纳入装箱批次、箱、箱明细、标准、标签和完成后变更。
- [x] F2 只复用 SC-F1 `SupplyPurchaseOrder`/`SupplyPurchaseOrderLine`。
- [x] F2 不建立平行采购单主数据。
- [x] 照片、视频、抽检审核归 F3。
- [x] 货柜、装柜、待装柜和发运归 F3。
- [x] F2 不执行 `ready_to_ship` 状态动作。
- [x] F2 不接对象存储、打印机或真实外部服务。
- [x] 名称相似但涉及货柜的 box change 功能未误纳入 F2。

## 4. 数据和状态

- [x] 批次—采购单使用显式关联表。
- [x] 一个批次只允许一个租户和一个供应商。
- [x] 采购单必须处于 `production_completed`。
- [x] 一个采购单最多关联一个未取消装箱批次。
- [x] 多采购单能力默认关闭。
- [x] 供应商自助装箱能力默认关闭。
- [x] 空箱禁止。
- [x] 超量禁止，服务端不得静默截断。
- [x] 完成时每条采购单行必须精确装完。
- [x] `completed` 后直接修改禁止。
- [x] 完成后变更使用申请、异人审核、原子应用和版本递增。
- [x] `pending_loading` 未进入 F2 状态枚举。

## 5. 权限、范围和审计

- [x] 内部最小权限代码已冻结。
- [x] DataScope 配置键已冻结。
- [x] 权限存在但无 DataScope 时拒绝。
- [x] 外部供应商使用账号绑定，不信任请求中的 `supplier_id`。
- [x] 停用或无效供应商绑定拒绝。
- [x] 小程序、普通 Web 和内部 API Token 通道隔离。
- [x] 状态动作、箱变更、标签和变更审核写入审计事件。
- [x] 审计事件不可更新、删除或绕过领域服务批量创建。
- [x] 响应字段最小化要求已冻结。

## 6. API 和客户端

- [x] 内部 API 前缀已冻结。
- [x] 供应商网页端 API 前缀已冻结。
- [x] 原生小程序 API 前缀已冻结。
- [x] 写接口要求 `Idempotency-Key`。
- [x] 幂等键负载冲突返回 `409`。
- [x] 越权对象不得泄露存在性。
- [x] Vue 内部端与供应商端页面范围已列明。
- [x] 原生小程序页面范围已列明。
- [x] 小程序不迁移 Taro 运行时。
- [x] 标签使用即时 PDF，不持久化。

## 7. MySQL 与测试门禁

- [x] 固定事务锁顺序已定义。
- [x] 并发创建批次场景已列入测试。
- [x] 并发箱序号场景已列入测试。
- [x] 并发装箱超量场景已列入测试。
- [x] 并发完成与箱修改场景已列入测试。
- [x] 并发批准变更场景已列入测试。
- [x] ORM `update/bulk_update/bulk_create/save/delete` 绕过场景已列入测试。
- [x] MySQL 空库升级、基线升级和回滚已列入测试。
- [x] 所有测试只允许 Mock 或虚构数据。

## 8. 风险与结论

| 级别 | 数量 | 说明 |
|---|---:|---|
| P0 | 0 | 未发现生产连接、真实数据或线上变更 |
| P1 | 0 | 首轮立项审核的 5 项待决策均已在主契约冻结 |
| P2 | 0 | 命名、源历史反模式和客户端边界已纳入契约 |

审核结论：

`PASS_FOR_LOCAL_MODEL_DEVELOPMENT`

允许的下一步：

- 创建 `packing` Django 应用；
- 建立模型和 MySQL migration；
- 建立领域服务骨架；
- 补充模型、并发、权限和 ORM 防绕过测试。

仍未授权：

- API 和客户端功能直接进入完成态；
- 源数据或真实数据迁移；
- 连接线上供应链系统；
- 小程序上传或发布；
- F3 装柜、发运和照片审核功能；
- 任何生产部署、双写、同步或切流。
