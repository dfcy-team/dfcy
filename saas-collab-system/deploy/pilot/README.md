# 阶段3生产试点安装包

本目录是阶段3生产试点的安装模板，不是正式生产发布包。

- `database/`：MySQL 8 和 Redis 的私有网络部署模板。
- `application/`：Django、Celery worker 与 beat 的试点部署模板。

先部署数据库端并通过健康检查，再部署应用端。真实 `.env.pilot` 文件只能由密钥托管系统或受控运维主机生成，绝不能提交到 Git。

本安装包不启用真实平台接入、真实 RPA、自动采购、自动改价、自动清仓、自动资金操作。
