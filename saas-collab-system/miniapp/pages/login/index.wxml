<view class="page login-page">
  <view class="brand">
    <view class="brand-mark">协</view>
    <view class="page-title">SaaS 协同小程序</view>
    <view class="page-subtitle">
      统一会话、请求、状态与发布治理的移动入口
    </view>
  </view>

  <view class="card">
    <view class="environment-row">
      <text>运行环境</text>
      <text class="environment-tag">{{environment}}</text>
    </view>
    <view wx:if="{{mock}}" class="mock-notice">
      当前为 Mock 模式，不连接真实账号、订单或生产平台。
    </view>
    <view wx:if="{{errorMessage}}" class="error-message" role="alert">
      {{errorMessage}}
    </view>
    <button
      class="primary-button login-button"
      loading="{{loading}}"
      disabled="{{loading}}"
      bindtap="handleLogin"
    >
      {{loading ? "正在登录" : "微信快捷登录"}}
    </button>
  </view>

  <view class="security-note">
    小程序只提交一次性登录凭证；平台密钥与 session_key 仅允许在服务端保存。
  </view>
</view>
