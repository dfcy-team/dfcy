{
  "name": "saas-collab-system-miniapp",
  "version": "0.1.0",
  "private": true,
  "description": "微信小程序工程底座",
  "scripts": {
    "validate": "node scripts/validate-project.js",
    "test": "node --test tests/*.test.js",
    "build": "npm run validate && npm test",
    "check": "npm run build",
    "https:proxy": "node scripts/local-https-proxy.js"
  },
  "engines": {
    "node": ">=18"
  }
}
